# 电机堵转检测重构

## 状态机

```
IDLE ──(检测触发)──> RETREATING ──(走到目标/超时)──> LOCKED ──(UNLOCKED)──> IDLE
```
整体逻辑

假设J2堵转：
J2 检测到堵转
    │
    ▼
J2 CAN广播 "STALL nodeID=2"（命令码 0x5A 广播）；J2电机RETREATING回退，回退完LOCKED
    │
    ├─→ 主控收到：停止下发新命令，切换到堵转模式，发送串口打印堵转，堵转电机等参数
    ├─→ J1/J3-J6/地轨 收到：切换到LOCKED，等待主控指令
    │
    │
    ▼
用户发送 enable/disable（给对应轴或全局）
    │
    ▼
主控 CAN广播 "UNLOCKED" 命令
    │
    ▼
所有电机恢复正常IDLE 状态

### IDLE 状态

**功能**：正常运行 + 20kHz 堵转检测（仅位置模式）

**具体动作**：

- 正常执行所有命令（位置、速度、力矩）
- **仅位置模式（MODE_COMMAND_POSITION）下执行堵转检测**；力矩/速度模式不检测
- 每 50μs 执行一次堵转检测
- **三条件判定**（三条件同时满足才算上升沿）：
  - 电流 > `stallCurrentThr`（FOC 输出的 q 轴电流（滤波后），单位 mA，`stallCurrentThreshold = ratedCurrent × 40%`）
  - 速度 < 50 step/s（`stallVelocityThr`，容忍加减速末段低速段）
  - 位置误差 > 15 step（`stallErrorThr`，极敏感）
- 三条件同时满足并持续 **30ms**（600 次循环，20kHz 下）→ 触发 Recheck
- **Recheck**：30ms 后再次判定，电流仍 > `thr × 60%`（即 `ratedCurrent × 24%`）、误差仍 > 12 step → 切换到 RETREATING
- 若 Recheck 时电机速度 > 150 step/s（明显加速）→ 清零上升沿，放弃触发

**Recheck 设计意图**：启动电流尖峰（持续 50~200ms）若 30ms 内回落 + 速度已上升 → 上升沿被清 → 不触发。解决了 v2 "200ms 启动误触"问题。

> ⚠️ **实现更新（2026-08-30，激进方案 v3）**：
> - 阈值改为硬编码在 `Ctrl/Motor/motor.cpp`（35/42/57 三套固件同步）
> - `stallCurrentThreshold` 在 `motorInit` 和 CAN `SET_CURRENT_LIMIT` 时根据 `ratedCurrent × 40%` 计算
> - 详细调参历史见 § 启动电流尖峰防御方案 → "当前决策"

**堵转检测开关**：

- `!STALL_DIS`：临时关闭堵转检测
- `!STALL_EN`：重新开启堵转检测
- `!STALL_STATUS`：查询各电机堵转状态
- 默认开启，不写 EEPROM

### RETREATING 状态

**功能**：电机按预设参数反向回退

**具体动作**：

1. 计算回退目标位置
2. 启动梯形速度曲线回退
3. 等待回退完成或超时
4. 切换到 LOCKED

**回退参数**：

| 参数       | 值               | 说明                          |
| -------- | --------------- | --------------------------- |
| 速度       | **50000 step/s** | 恒定速度                        |
| 加减速曲线    | 梯形              | 加速 + 匀速 + 减速                |
| 回退时间     | ≈1s             | 35556步 / 50000 + 加速/减速         |
| 关节电机回退距离 | 35556 step      | J1~J5 约 5°，J6 约 8.33°         |
| 地轨电机回退距离 | 51200 step      | 约 5mm                       |
| 超时       | 2s              | 超时后直接进入 LOCKED，不管是否走完         |

**梯形曲线计算**（以加速度 100000 step/s² 为例）：

- 加速阶段：50000 / 100000 = 0.5s
- 匀速阶段：(35556 - 25000) / 50000 = 0.21s
- 减速阶段：0.5s
- 总时间 ≈ 1.21s

**超时机制**：

- 回退开始时记录起始时间
- 2 秒内未完成 → 强制进入 LOCKED

**回退方向计算**：

```cpp
retreatDirection = (goalPosition - estPosition) > 0 ? +1 : -1;
retreatTarget = estPosition - retreatDirection * retreatSteps;
```

向减小误差的方向回退。

**RETREATING 期间行为**：

- **关闭堵转检测**：触发回退后不重新进入检测循环，必须等用户 enable/disable 才能重置 stallMode
- **持续周期性广播 STALL**：每 250ms 一次（10ms × 25 次），回退期间持续发，**LOCKED 后停止发送**

**250ms 重发机制实现**：

- 在 100Hz 中断（htim1，10ms 周期）里加计数器
- 计数器累计到 25（约 250ms）→ 发一次 STALL 广播
- `stallMode == RETREATING` 才计时，其他状态计数器清零
- 现有 `Tim1Callback100Hz()` 里已经有按钮、LED 处理，加这里最合适
- 不影响 20kHz 闭环控制（htim4）
- 总线负载估算：8 电机同时堵转 × 4 msg/s = 32 msg/s，远低于 500kbps CAN 总线容量

### 回退完成的判定

**复用主控 MoveJ 完成判定标准**（`dummy_robot.cpp` L536-545 `IsMoving()`）：

```cpp
bool DummyRobot::IsMoving()
{
    static constexpr float EPSILON_DEG = 1.0f;  // 容差 1°
    for (int i = 1; i <= 6; i++)
    {
        if (fabsf(motorJ[i]->angle - motorJ[i]->targetAngle) > EPSILON_DEG)
            return true;
    }
    return false;
}
```

**电机端判定**（在 20kHz 主循环里，stallMode==RETREATING 时检查）：

> ⚠️ **实现修正（2026-08-25）**：文档原写"复用 state==STATE_FINISH 判定"，但实际实现时发现，
> `Update State` 中 RETREATING/LOCKED 会将 `state` 覆盖为 `STATE_STALL`（在 RETREATING handler 之后执行），
> 导致 RETREATING handler 中读取的 `state` 永远是旧值，无法正确判定。
> **修正方案**：直接用原始变量判定，不依赖 state。

```cpp
if (controller->stallMode == RETREATING)
{
    // 不依赖 state（会被 Update State 覆盖为 STATE_STALL）
    // 直接用原始变量判定回退是否完成
    bool reachedGoal = (controller->modeRunning == MODE_COMMAND_POSITION) &&
                       (controller->softPosition == controller->goalPosition) &&
                       (controller->softVelocity == 0);
    if (reachedGoal)
    {
        controller->stallMode = STALL_LOCKED;
    }
}
```

**说明**：
- 直接用 `softPosition == goalPosition && softVelocity == 0` 判定（梯形/S曲线到位标准）
- 不依赖 `state`（`state` 会被 `Update State` 中的 `STATE_STALL` 覆盖）
- 2s 超时兜底：如果一直不满足条件，2s 后强制进 LOCKED

### 超时强制 LOCKED（2s 兜底）

**背景**：堵转检测 + 回退可能因为各种原因卡住（编码器数据异常、回退方向错误等），必须有兜底机制。

**实现**：

```
// 在 stallMode == RETREATING 状态中检查超时
// 2s 超时强制 LOCKED
if (HAL_GetTick() - controller->retreatStartTick > STALL_TIMEOUT_MS)
{
    controller->stallMode = LOCKED;
    // 超时也要发 STALL 广播（最后一帧）
    CAN_StallBroadcast(nodeID, STALL_TIMEOUT);
}
```

**2s 超时的合理性**：

- 正常回退 35556 步需要时间：≈1.21s（按梯形曲线 50000 step/s 速度 + 100000 step/s² 加速度）
- 2s = 1.65× 正常回退时间 → 留 65% 余量
- 但不能太长，否则用户以为堵转处理失灵

### LOCKED 状态

**功能**：保持当前位置（PID 闭环），不响应新位置命令

**具体动作**：

- 持续 PID 闭环控制，保持当前姿态
- 不响应任何新位置命令
- **只响应 enable/disable，其他串口命令忽略**
- 等待主控的 `UNLOCKED` 广播
- **LOCKED 期间停止 STALL 周期性广播**（已经发了 STALL_DONE 通知主控）

**退出 LOCKED**：

| 命令      | 行为                                         |
| ------- | ------------------------------------------ |
| enable  | 清除 stallMode + 重新初始化 MotionPlanner，电机恢复正常控制 |
| disable | 清除 stallMode + 重新初始化 MotionPlanner，电机失能      |

> enable/disable 由主控统一广播 `UNLOCKED` 全局命令。

**LOCKED 状态下 enable 的具体实现**（`UserApp/protocols/interface_can.cpp` 0x01 处理）：

```cpp
case 0x01:  // Enable Motor
    if (*(uint32_t*)(RxData) == 1)
    {
        motor.controller->requestMode = Motor::MODE_COMMAND_VELOCITY;
        // 1. 清除 stallMode → 退出 LOCKED
        motor.controller->stallMode = IDLE;
        // 2. 【关键】不恢复原 goalPosition！
        //    触发堵转的原因 = 用户原目标本身不合理（位置超限、物理卡死、目标错误）
        //    如果恢复原目标 → 电机再次卡向同一位置 → 再次堵转 → 死循环
        //    所以抛弃原目标，goalPosition 保持当前 retreatTarget 位置
        // 3. 用当前位置作为 MotionPlanner 新起点
        //    此时 goalPosition = retreatTarget = 当前位置（回退后的位置）
        //    NewTask(estPosition) 后 estError = softPosition - estPosition = 0
        //    → 电机认为"已到位"，不再主动向任何方向运动
        // 4. 等待用户下个新位置命令（新的 MoveJ/MoveL）
        motionPlanner.positionTracker.NewTask(estPosition, estVelocity);
        // 5. 清堵转检测累积时间
        motor.controller->stalledTime = 0;
    }
    else
    {
        // disable 路径类似，但 requestMode = MODE_STOP
        motor.controller->requestMode = Motor::MODE_STOP;
        motor.controller->stallMode = IDLE;
        motionPlanner.positionTracker.NewTask(estPosition, 0);
    }
    break;
```

**关键决策**：

- **抛弃原 goalPosition**：触发堵转的原目标本身就不合理（位置超限、物理卡死），不能恢复
  - 如果恢复 → 电机再次卡向同一位置 → 再次堵转 → 死循环
  - 抛弃后 goalPosition = retreatTarget = 回退后的位置

- **enable 后电机停在当前位置**：motionPlanner.NewTask(estPosition) 后 estError = 0，电机"已到位"，不再主动运动

- **等待用户下个新位置命令**：原 MoveJ 已经被"部分完成"或"放弃"，用户需要重新发命令

- **不发 UNLOCKED 广播**：主控是发起方，不需要电机再广播回去

### 抛弃原目标 vs 保留原目标的对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| 抛弃原目标（推荐）| 不会死循环 | 用户 MoveJ 不再"完整执行"，主控不知道 | 真实堵转（边界、物理卡死） |
| 保留原目标 | 主控 IsMoving 完整执行 | 死循环风险 | 仅当堵转是临时干扰（不会持续）|

**真实场景分析**：

- **位置超限**：地轨 260mm 超 250mm 上限 → 永远到不了 → 必须抛弃
- **物理卡死**：电机被外物卡住 → 即使移除外物，原目标可能仍可达 → 但安全起见也抛弃
- **临时干扰**：电机瞬间被卡 → 30ms 后干扰消失 → 这种情况保留原目标可以继续执行

**为什么还是推荐抛弃**：

- 难以区分"临时干扰"和"真实死锁"
- 主控有 1° 容差 `IsMoving()`，抛弃后主控很快收到"ok"，用户可重发命令
- 死循环风险不可接受

### 急停优先级

急停命令（0x89）优先于堵转检测，急停期间暂停堵转检测。

## 检测逻辑（每 50μs 执行一次）

**三个条件同时满足**才算堵转：

1. **电流大** — 电机在使劲（电流超过了预设阈值）
2. **速度低** — 实际转不动或转得很慢（速度低于阈值）
3. **误差大** — 目标位置和实际位置差很多（说明在发命令让电机走，但走不动）

**三个条件必须同时满足，并持续 30ms 才触发回退**（详见 § 速度和误差阈值）

### 为什么要三个条件？

- 只有电流大 → 可能是启动加速阶段（电流会飙高）
- 只有速度低 → 可能是停在目标位置等下一个命令
- 只有误差大 → 可能是刚接到新命令，还没走完
- **三个同时** → 才是真正的"想吃劲、但转不动、走不到" = 堵转

### 计时器怎么工作的？

每次循环检测到三个条件都满足 → 计时器 +50μs
任意一个条件不满足 → 计时器清零

计时器累计到 30ms（600 次循环）→ 触发 Recheck（详见 § 启动电流尖峰防御方案）

## 触发回退后发生什么？

1. 计算回退目标位置（当前位置 - 回退步数）
2. 电机开始反向走
3. 状态切到 RETREATING（回退中）
4. 等电机走到目标位置
5. 状态切到 LOCKED（锁死）
6. LOCKED 状态下电机保持当前位置和力矩，不再响应任何新位置命令
7. 用户发 enable 或 disable 命令 → 退出 LOCKED，电机恢复正常

### 触发瞬间的具体动作（20kHz 循环内）

当三个堵转条件持续满足 30ms 时，在 20kHz 循环内立即执行以下动作：

```cpp
// 触发瞬间（stallMode: IDLE → RETREATING）
controller->retreatStartTick = HAL_GetTick();  // 记录开始时间

// 计算回退目标：向减小误差的反方向走
controller->retreatDirection = (controller->goalPosition - controller->estPosition) > 0 ? +1 : -1;
controller->retreatTarget = controller->estPosition - controller->retreatDirection * retreatSteps;

// 设置回退目标，启动 motionPlanner 梯形回退
SetPositionSetPoint(controller->retreatTarget);
motionPlanner.positionTracker.NewTask(controller->estPosition, controller->estVelocity);

// 切换状态
controller->stallMode = RETREATING;

// 立即发第 1 次 STALL 广播（之后 250ms 周期由 100Hz 中断重发）
CAN_StallBroadcast(nodeID, STALL_TRIGGER);
```

**关键**：触发瞬间就用 `estPosition` 快照计算 `retreatDirection`，不再每帧重算，避免 `retreatTarget` 来回跳。

### RETREATING 超时机制

回退过程中如果电机卡住（比如 2 秒内走不完），直接进入 LOCKED，不再等待。

## 回退参数

### 关节电机（J1~J6）

51200 × 50（减速比） × (5/360) = 35556 步


| 轴      | 减速比  | 35556 步对应输出轴角度 | 说明                  |
| ------ | ---- | -------------- | ------------------- |
| J1~J5  | 50:1 | **5°**         | 标准回退                |
| J6     | 30:1 | **8.33°**      | 减速比小，相同步数回退角度更大，可接受 |


### 地轨电机


| 电机    | 步数      | 对应距离    |
| ----- | ------- | ------- |
| 57 地轨 | 51200 步 | **5mm** |


> 计算：丝杆 1605 直连，1 圈 = 5mm，51200 步/圈（200 步 × 256 微步）

## 电流阈值

使用电机内部的 `currentLimit`（用户通过串口助手设定的电流限幅值）

不是固定值。当用户降低电流时，堵转检测阈值也会相应降低。

**首触发阈值** = `currentLimit × 40%`（40% 是安全系数，避免启动峰值误触）

**Recheck 阈值** = `currentLimit × 40% × 60% = currentLimit × 24%`（放宽 60%，30ms 后电流已回落则放行）

**典型电流对应的阈值表**：

| `currentLimit` | 首触发阈值 | Recheck 阈值 | 适用场景 |
|---|---|---|---|
| 0.2A | 80mA | 48mA | 手可触发验证 |
| 0.5A | 200mA | 120mA | 常用工作电流 |
| 1.0A | 400mA | 240mA | 中等负载 |
| 2.0A | 800mA | 480mA | 重载（地轨 57） |

**赋值时机**：
- `motorInit()` 中：`stallCurrentThreshold = (int32_t)(ratedCurrent × 40 / 100);`
- CAN `SET_CURRENT_LIMIT` 中（interface_can.cpp L155）：同样公式重算

## 退出 LOCKED 行为

LOCKED 状态下收到命令：


| 命令      | 行为                           |
| ------- | ---------------------------- |
| enable  | 清除 stallMode + 重新初始化 MotionPlanner，电机恢复正常控制 |
| disable | 清除 stallMode + 重新初始化 MotionPlanner，电机失能      |


> 重新初始化 MotionPlanner 原因：LOCKED 期间 MotionPlanner 状态是旧的，退出时需要用当前位置/速度重置，避免新命令接到旧状态上。

## 堵转检测开关

**默认开启**，可通过串口临时关闭/开启：


| 命令              | 功能        |
| --------------- | --------- |
| `!STALL_DIS`    | 临时关闭堵转检测  |
| `!STALL_EN`     | 重新开启堵转检测  |
| `!STALL_STATUS` | 查询各电机堵转状态 |

**`!STALL_STATUS` 返回格式**（**已对齐串口助手 2026-08-23 实现的解析逻辑**）：

```
ok STALL_STATUS rail_en=1 j1_en=1 j2_en=1 j3_en=1 j4_en=1 j5_en=1 j6_en=1 \
                 rail_lock=0 j1_lock=0 j2_lock=0 j3_lock=0 j4_lock=0 j5_lock=0 j6_lock=0
```

| 字段 | 含义 | 取值 |
|------|------|------|
| `rail_en` | 地轨堵转检测是否开启 | 0=关 / 1=开 |
| `j{1~6}_en` | J1~J6 堵转检测是否开启 | 0=关 / 1=开 |
| `rail_lock` | 地轨是否处于 LOCKED 状态 | 0=否 / 1=是 |
| `j{1~6}_lock` | J1~J6 是否处于 LOCKED 状态 | 0=否 / 1=是 |

**实现要点**：
- **主控端**：收到 `!STALL_STATUS` → 通过 CAN 命令查询每电机的 `stallMode` 和 `stallProtectSwitch` → 组装响应字符串 → UART 返回
- **CAN 命令码**：`0x5C`（新增），Data[0]=查询类型（1=en 状态，2=lock 状态），由主控单播各电机
- **电机端**：收到 `0x5C` → 返回当前 `stallProtectSwitch`（对应 en）或 `stallMode == LOCKED`（对应 lock）

**已实现情况说明**：
- 电机端已实现 `0x5C` 命令码（interface_can.cpp）
- 主控端已实现 `!STALL_STATUS` 处理（ascii_protocol.cpp），但目前返回硬编码状态字符串，未收集电机实际响应
- 串口助手已有 UI 解析

> 不写 EEPROM，重新上电后自动恢复开启

### 启动电流尖峰防御方案（**待实测验证**）

**问题背景**：

| 阶段 | 电流特征 | 速度特征 | 误差特征 | 三条件同时满足？ |
|------|---------|---------|---------|----------------|
| 启动加速 0~30ms（前段） | 1500~2000 mA（>800mA @ 2A 阈值，或 >80mA @ 0.2A）| 0→50000 step/s（早期 <50 step/s） | 大 → 小（>15 步）| **可能前 30ms 满足** |
| 30ms Recheck（启动场景）| 已回落至 500mA 左右（<200mA × 60% = 120mA @ 0.5A）| 已上升至 >150 step/s | 减小到 <12 步 | **不满足 Recheck → 上升沿清零** |
| 正常堵转 | 持续 > thr × 60%（24% × ratedCurrent）| 持续 <50 step/s | 持续 > 15 步 | **Recheck 满足** |

**30ms + Recheck 持续判定窗口是否够**：

- 启动尖峰持续时间：经验值 50~200ms（取决于电机惯量、负载）
- **风险**：尖峰若持续 ≥ 30ms 且 Recheck 时仍未回落 → 误触
- **当前防御**：Recheck 双条件（电流×60% + 速度 > 150 step/s 清零）已挡住绝大多数启动场景
- **结论**：30ms + Recheck 已可挡住 ≥ 80% 启动尖峰场景

**备用方案（实测继续出问题再启用）**：

| 方案 | 描述 | 改动量 | 副作用 | 状态 |
|------|------|--------|--------|------|
| **A. 启动保护期** | 进入位置模式后前 N ms（如 500ms）暂停堵转检测 | 加一个 `stallBlankTime` 计数器 | 启动阶段 N ms 内无法检测真实堵转 | 备用 |
| **B. 放宽速度阈值** | 速度阈值 ~~10 → 50 step/s~~（**已采纳**）| — | 灵敏度降低（v3 已实测可接受）| ✅ |
| **C. 放宽电流阈值** | 阈值 ~~75% → 40%~~（**已采纳**）；Recheck 60% | — | 灵敏度降低（v3 已实测可接受）| ✅ |
| **D. 加速度辅助判定** | 加条件 `加速度 < 阈值`（启动时加速，堵转时不加速） | 加 estVelocity 微分计算 | 计算量大，可能引入噪声 | 备用 |
| **E. 上升沿再判定** | ~~三条件首次满足时记录 t0，200ms 后再检查速度是否 < 10 step/s~~（**已采纳，简化为 30ms Recheck，velocity>150 step/s 清零**）| — | 实时性略降（v3 已实测可接受）| ✅ |

> **当前决策（v3 激进方案，2026-08-30 采纳）**：
> - 当前参数：`stallCurrentThr = ratedCurrent × 40%`、`stallVelocityThr = 50 step/s`、`stallErrorThr = 15 step`、持续 `30ms` + Recheck `电流 > 60% × thr & 误差 > 12`
> - 已被采纳方案：B + C + E（速度阈值放宽、电流阈值放宽、上升沿 + Recheck）
> - 历史变更：从 `75% × 10 step/s × 50 step × 200ms`（v1）→ `60% × 100 step/s × 30 step × 50ms`（v2）→ `40% × 50 step/s × 15 step × 30ms + Recheck`（v3 现行）
> - **实测结论**：0.2A 电流下手可触发（80mA 阈值），0.5A 电流下手无法抓握电机（物理原因，需刚性障碍物才能验证）
> - **下一步**：B/C/E 三方案已通过 0.2A 测试；0.5A+ 测试需刚性障碍物（手扳不动），暂未实测

## 速度和误差阈值

> **v3 激进方案参数（2026-08-30 采纳）**：

| 参数 | 阈值 | 说明 |
|------|------|------|
| `stallVelocityThr` | **50 步/s** | 比 v2 进一步放宽，容忍加减速末段低速段（实测 30 太严、背隙/抖动导致误判；50 容忍过渡段）|
| `stallErrorThr` | **15 步** | 比 v2（30）更敏感，误差积累少也能触发（50:1 减速约 0.04°，极敏感）|
| `stallRecheckErrorThr` | **12 步** | Recheck 阶段允许略放宽（防止 30ms 内误差波动清零）|
| `stallDurationUs` | **30000** (30ms) | 比 v2 进一步缩短，600 × 50μs 周期 |
| Recheck 速度清零 | **`\|estVelocity\| > 150 step/s`** | 比 v2（200）放宽 25%，容忍低速抖动 |

**清除条件**：电机确实在动（速度 > 150 step/s）时清零上升沿，避免缓动 / 加减速末段误触。

**阈值实现位置**：
- 直接硬编码在 `Ctrl/Motor/motor.cpp` 各三条件 + Recheck / 清零分支
- 35/42/57 三套固件同步采用相同参数
- `stallCurrentThreshold` 单独计算（按 `ratedCurrent × 40%` 见 § 电流阈值）

## LOCKED 保持位置

LOCKED 状态使用 PID 闭环保持位置，不是固定电流输出。

电机内部 PID 一直开着，LOCKED 只是不响应新位置命令，PID 继续调节保持当前姿态。

> ⚠️ **实现修正（2026-08-25）**：文档原写"LOSED 状态就是正常跑 PID 闭环保持位置"，
> 但实际代码中 LOCKED 分支仅调用了 `ClearIntegral()`，并未主动调用 `CalcDceToOutput()` 输出控制量。
> 这导致 `focCurrent` 保持在 RETREATING 末尾的值，或被其他路径清零，电机实际无法可靠锁位。
> **修正方案**：在 LOCKED 分支调用 `CalcDceToOutput(estPosition, 0)`，以当前位置为 setpoint，velocity=0，
> 主动输出 PID 闭环控制量维持位置。

## CAN 状态回传

### 电机端行为

1. 检测到堵转 → CAN 广播 `STALL nodeID=x`（命令码 `0x5A`，Data[0]=nodeID, Data[1]=1=trigger）
2. 回退期间每 250ms 重发一次 STALL（Data[1]=1）
3. 回退完成 → 广播 `STALL_DONE nodeID=x`（命令码 `0x5A`，Data[0]=nodeID, Data[1]=2=done）
4. 进入 LOCKED 后停止 STALL 周期性广播

### 主控端行为

1. 收到 `STALL nodeID=x`（Data[1]=1）→ 停止下发新命令，打印堵转信息（轴号、电流、速度、误差）
2. 收到 `STALL_DONE nodeID=x`（Data[1]=2）→ 确认堵转完成

### 所有电机行为（收到 STALL 广播后）

**触发堵转的电机（自己发 STALL）**：

| 状态 | 位置命令 0x05/0x07 | enable/disable 0x01 | 急停 0x89 | 其他（力矩/速度/查询）|
|------|---------------------|---------------------|-----------|---------------------|
| IDLE → RETREATING 切换瞬间 | **丢弃**（已在执行回退规划）| **接受**（见下）| 接受 | 接受（力矩/速度绕过堵转检测）|
| RETREATING 中 | **丢弃** | **接受，清除 stallMode → 重置 MotionPlanner 到当前位置（放弃回退目标）** | 接受，强制进 LOCKED | 接受 |
| LOCKED | **丢弃** | **接受，清除 stallMode → 重置 MotionPlanner** | 接受，叠加 MODE_STOP + 抱闸（不清 stallMode）| **丢弃**（除 enable/disable 和急停外）|

**其他电机（收到他人 STALL 广播）**：

| 状态 | 位置命令 0x05/0x07 | enable/disable 0x01 | 急停 0x89 | 其他 |
|------|---------------------|---------------------|-----------|------|
| 正常（未触发 stallMode）| **丢弃**（主控已停止下发）| **接受**（行为同 IDLE）| 接受 | 接受 |
| LOCKED（与堵转电机同步锁定）| **丢弃** | **接受，恢复正常控制** | 接受，叠加 MODE_STOP + 抱闸 | **丢弃**（除 enable/disable 和急停外）|

> **关键说明**：
> 1. 其他电机**不加新状态机**，复用 `stallMode` 枚举（IDLE / LOCKED），但**不会进入 RETREATING**（因为不是堵转源）
> 2. 其他电机收到 STALL 广播时，`stallMode` 直接置为 `LOCKED`，跳过 RETREATING（因为没有"回退目标"的概念）
> 3. 急停 0x89 是 **CAN 总线急停**，进入 MODE_STOP 但**不重置 stallMode**，等 enable/disable 才能解除
> 4. enable/disable 由主控发 0x5B UNLOCKED 广播，等同于"用户发 enable/disable → 主控连续 3 次发 UNLOCKED"

### 解除堵转

用户发送 enable/disable → 主控 **连续发送 3 次** `UNLOCKED` 广播（命令码 `0x5B`，Data 全部填 0，间隔 100ms 防丢包）→ 所有电机恢复正常模式

### CAN 命令码分配（**已决策**，2026-08-25 更新：避开位宽冲突）

> ⚠️ **位宽冲突修复（2026-08-25）**：
> 原有 0x8A/0x8B/0x8C 在电机端 `can.c` 中解析时使用 `cmd = StdId & 0x7F`（取低 7 位），
> 导致电机广播的 StdId `(nodeID << 7) | 0x8A`（例如节点 9 发 0x48A）被解析为 `cmd=0x0A`（低 7 位丢失第 8 位），
> 所有广播命令均无法命中 `case 0x8A`！解决方案：将命令码迁移至 0x50~0x7F 区间（避开 0x80 以上广播区间的位宽混淆），
> 并将电机端广播判断阈值从 `0x80~0xBF` 扩展为 `cmd >= 0x50`。


| 命令码 | 用途 | 备注 |
|------|------|------|
| `0x89` | 广播急停 | 已存在 |
| `0x5A` | 广播 STALL / STALL_DONE | **新增**，Data[0]=nodeID, Data[1]=1=trigger / 2=done / 3=timeout |
| `0x5B` | 广播 UNLOCKED | **新增**，Data 全部填 0，只支持全局解除 |
| `0x5C` | 单播查询 stall 状态 | **新增**，Data[0]=1=en / =2=lock，主控→单电机 |
| `0x1B` | 切换 stallProtectSwitch | 已存在，**保留**，对应 `!STALL_EN` / `!STALL_DIS` |
| `0x7C` | ~~旧 STALL 上报~~ | **废弃**，删除 |

### 旧堵转逻辑清理

重构时一并删除以下旧代码：

- `isStalled` 布尔标志（motor.h L121, motor.cpp 多处）
- `stalledTime` 字段（motor.h L164）
- `ClearStallFlag()` 函数（motor.cpp L538）
- `STATE_STALL` 状态
- 命令码 `0x7C`（旧 STALL 上报）
- 电机端 95% ratedCurrent 阈值逻辑（已替换为 v3 `stallCurrentThr = ratedCurrent × 40%`）
- 旧堵转状态机的 if/else 分支（motor.cpp L105、L324）

**保留**：
- `stallProtectSwitch` 配置（motor.h L22, L104）—— 改为 `stallProtectSwitch`，仅作为"是否检测"的开关，**不存 stallMode 状态**
- 命令码 `0x1B`（Set Enable Stall-Protect）—— 保留，对应 `!STALL_EN` / `!STALL_DIS`，不写 EEPROM

只保留新 `stallMode` 枚举（IDLE / RETREATING / LOCKED）作为唯一状态来源。
`stallProtectSwitch` 仅是"是否检测"的开关，不替代 stallMode 状态机。

### 急停与 LOCKED 交互

**急停独立逻辑，不复用 stallMode/LOCKED**，但行为部分一致：

| 行为 | LOCKED | 急停 |
|------|--------|------|
| 不响应位置/速度/力矩命令 | ✅ | ✅ |
| 需 enable 解除 | ✅ | ✅（当前逻辑） |
| PID 闭环保持位置 | ✅ | ❌（急停要求 0 输出 + 抱闸） |
| 抱闸 (driver.Brake) | ❌ | ✅ |
| 周期性广播 | ✅（STALL 重发） | ❌ |


### LOCKED 状态下收到急停

- 电机进入 `MODE_STOP` + `SetBrake(true)` + 清零 velocity/current，但 **不退出 LOCKED**
- 因为 LOCKED 状态不响应位置命令，急停额外叠加抱闸不会改变已经停止的事实
- LOCKED 状态下收到 enable：清除 stallMode + 重置 MotionPlanner 到当前位置，电机停在当前位置等待用户下个新命令

---

## 数据结构变更

### motor.h 新增字段

```cpp
struct Controller {
    // ... 现有字段 ...
    
    enum StallMode_t {
        IDLE = 0,       // 正常状态
        RETREATING = 1, // 回退中
        LOCKED = 2,     // 锁死状态
    };
    StallMode_t stallMode = IDLE;  // 替换原 isStalled bool
    
    uint32_t stallBroadcastTick{}; // 250ms 周期广播计数器
    uint32_t retreatStartTick{};   // 回退开始时刻（2s 超时用）
    
    // 注意：不需要 lockedGoalPosition 备份
    // 触发堵转后抛弃原目标，LOCKED 期间 goalPosition = retreatTarget（回退后的位置）
    // enable 时也保持这个 goalPosition，等待用户下个新位置命令
    
    // ... 其他字段 ...
};
```

### 状态机常量

| 常量 | 值 | 含义 |
|------|---|------|
| `IDLE` | 0 | 正常，检测堵转 |
| `RETREATING` | 1 | 回退中，250ms 广播 + 等待完成 |
| `LOCKED` | 2 | 锁死，不响应位置命令，等 enable |
| `RETREAT_EPSILON` | 100 步 | 回退完成容差（≈1°）|
| `STALL_TIMEOUT_MS` | 2000 ms | 回退超时强制 LOCKED |
| `STALL_BROADCAST_MS` | 250 ms | 周期广播间隔 |

---

## 完整时序总结

### 场景 A：堵转后正常恢复（用户下个新命令）

```
T=0    用户发 MoveJ → goalPosition = 90°（原目标）
T=0~1s 电机正常运动 0° → 89.95°
T=1s   触发堵转（位置/物理卡死等原因）
T=1s   抛弃原目标 90°
T=1s   goalPosition = retreatTarget = 84.95°
T=1~2s 梯形回退中（250ms 周期广播 STALL）
T=2s   回退完成 → LOCKED → 发 STALL_DONE 广播
T=2~3s 等待 enable（电机停在 84.95°，不响应位置命令）
T=3s   收到 enable
        stallMode = IDLE
        goalPosition 保持 84.95°（抛弃原目标）
        motionPlanner.NewTask(84.95°, 0) → estError = 0 → 电机"已到位"
T=3s   主控 IsMoving() 返回 false
T=3s   主控打印 "ok"（其实原目标已被抛弃，电机停在 84.95° 而非 90°）
        这是【设计取舍】：为了避免边界死循环，抛弃原目标
        用户需要意识到 MoveJ 可能"未完整执行"，需要重发命令
T=3~10s 电机停在 84.95° 等待用户下个新命令
T=10s  用户发 MoveJ → goalPosition = 80°（新目标）
        motionPlanner.NewTask(84.95°) → 从当前位置到新目标
T=10~12s 84.95° → 80° 插值
T=12s  state = FINISH → 主控 IsMoving() 返回 false
```

### 场景 B：边界死锁（你的反例，避免恢复原目标）

```
T=0    用户发 MoveJ → goalPosition = 260mm（超地轨上限 250mm）
T=0~3s 电机正常运动 0mm → 250mm
T=3s   触发堵转（卡到边界）
T=3s   抛弃原目标 260mm（这是死锁的关键：260mm 物理不可达）
T=3s   goalPosition = retreatTarget = 245mm
T=3~4s 梯形回退 250mm → 245mm
T=4s   回退完成 → LOCKED → 发 STALL_DONE 广播
T=4~5s 等待 enable（电机停在 245mm）
T=5s   收到 enable
        stallMode = IDLE
        goalPosition 保持 245mm（不恢复 260mm）
        motionPlanner.NewTask(245mm) → estError = 0 → 电机"已到位"
        → 不会再次向 260mm 推进 → 不会再次堵转
T=5s   主控 IsMoving() 返回 false
T=5s   主控打印 "ok"
T=5~10s 电机停在 245mm 等待用户下个新命令
T=10s  用户发 MoveJ → goalPosition = 200mm（新目标，物理可达）
        motionPlanner 正常规划 245mm → 200mm
T=10~12s 完成
```

**场景 B 关键**：**如果 enable 时恢复 260mm → 电机再次卡到 250mm → 再次堵转 → 死循环。**

---





## 待补充的方案细节（评审阶段发现，需重构前先达成共识）

以下是评审原方案时发现的遗漏 / 不严谨之处，**重构前必须先就这些点达成共识**，否则编码时容易反复返工或埋雷。每点用大白话说明 **问题是什么 → 会出什么后果 → 建议怎么处理**。

### 补强 1：主控端 CAN 接收处理 + targetAngle 同步

**问题**：

原方案给电机端定义了一堆新命令码（0x8A 广播堵转、0x8B 解除、0x8C 状态查询），但**主控端 `can_protocol.cpp` 怎么响应这些消息完全没说**。当前代码里只处理了 0x7C（旧堵转上报），而 0x7C 是要废弃的。

**建议方案**：

主控 `can_protocol.cpp` 中收到 `0x8A` 广播时，调用 `SetStallMode(nodeID)`：

```
收到 0x5A（广播堵转）：
  data[0] = 发起节点ID（哪个电机堵了）
  data[1] = 1:TRIGGER / 2:DONE / 3:TIMEOUT

  TRIGGER：
    1) dummy.SetStallMode(节点号)   ← 切红色心跳 + 停发新指令 + 同步 targetAngle + 清 FIFO
    2) 串口打印 [STALL] node=2 ...

  DONE / TIMEOUT：
    1) dummy.SetStallMode(节点号)   ← 再次同步 targetAngle（回退后的精确位置）
    2) 串口打印 [STALL] node=2 done

主控 ASCII 协议额外加两条命令：
    !STALL_UNLOCK → 主控发 0x5B 广播，连续发 3 次（间隔 100ms 防丢包）
    !STALL_STATUS → 顺序查询地轨+J1~J6 7 个电机的 en/lock 状态，拼字符串返回
```

**`targetAngle` 同步原理**（`SetStallMode` 实现，`dummy_robot.cpp` L430-442）：

电机端会持续通过 `0x23` ACK 回传当前位置（`GetPosition()` = `estPosition`），主控收到后更新 `motorJ[i]->angle` → `currentJoints` 刷新。`SetStallMode()` 直接用 `currentJoints` 设置 `targetAngle`，所以 `IsMoving()` 在收到下一帧 0x23 后立即返回 false，MoveJ 阻塞循环退出。**无需新增 CAN 命令**，复用现有 0x23 机制即可。

**急停后行为**：

LOCKED 状态下收到急停：电机进入 `MODE_STOP` + `SetBrake(true)` + 清零 velocity/current，但 **不退出 LOCKED**。急停期间堵转检测已被关闭，收到 `UNLOCKED`（enable）时，`motionPlanner.NewTask()` 的起点是**急停后的当前位置**，和正常退出 LOCKED 行为一致。

**夹爪处理**：

夹爪（CAN ID=8）是独立固件，不参与堵转检测，也不需要接受 `UNLOCKED`。堵转检测只涉及地轨+J1~J6 共 7 个电机。

**优先级**：P0 必须先于编码实现，**否则 MoveJ 必卡死**。

### 补强 2：计时变量声明位置需明确

计时变量需明确声明位置，避免实现时漏掉或重复定义：

| 变量 | 声明位置 | 备注 |
|------|---------|------|
| `stallBroadcastCnt` | `motor.cpp` 文件顶部 `static` | 100Hz 中断计数用 |
| `retreatStartTick` | `motor.h` Controller 类内 | `HAL_GetTick()` 单位 ms |
| `stallMode` | `motor.h` Controller 类内 | 替换 `isStalled` |
| `retreatTarget`、`retreatDirection` | `motor.h` Controller 类内 | 触发瞬间快照 |
| `retreatSteps` | `motor.h` Controller 类内 | 回退步数（地轨/关节分别配置） |

---

## 重构过程中修复的 Bug

本章节记录在重构方案落地过程中发现并修复的 Bug。

### Bug-1：设置电流后堵转阈值未更新

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

用户通过 `#I_LIMIT_J 9 2.8` 设置地轨电流后，堵转阈值没有同步更新，导致堵转检测无法触发。

**根本原因**：

`stallCurrentThreshold` 仅在电机初始化时计算一次，后续设置电流时未同步更新。

**修复方案**：

在 `interface_can.cpp` 的 `0x12` 命令处理中，设置电流后同步更新 `stallCurrentThreshold`。

**涉及文件**：所有电机固件的 `interface_can.cpp`

---

### Bug-2：堵转检测默认关闭

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

电机固件编译后堵转检测默认处于关闭状态。

**修复方案**：

1. `motor.h`：`stallProtectSwitch` 默认值改为 `true`
2. `main.cpp`：初始化时强制覆盖为 `true`

**涉及文件**：所有电机固件

---

### Bug-3：临时关闭命令误写 EEPROM

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

发送 `!STALL_DIS` 临时关闭后被写入 EEPROM，重新上电后依旧关闭。

**修复方案**：

只有开启时才写入 EEPROM，关闭时只写 RAM。

**涉及文件**：所有电机固件的 `interface_can.cpp`

---

### Bug-4：启动阶段误触发堵转检测

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

电机启动加速阶段可能误触发堵转检测。

**修复方案**：

添加启动豁免期（进入位置模式后前 100ms 不检测）。

**涉及文件**：所有电机固件的 `motor.cpp`

---

### Bug-5：电机端 can.c 广播阈值导致 0x8x 命令码静默丢弃（P0 Critical）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

电机端 `can.c` 解析 CAN 帧时使用 `cmd = StdId & 0x7F`（取低 7 位），并用 `cmd >= 0x80` 判断广播。
导致电机固件新加的 0x8A/0x8B/0x8C（STALL/STALL_DONE/UNLOCKED/查询）全部无法命中广播分支：

- 电机 A 发 StdId = `(9 << 7) | 0x8A = 0x48A`
- 接收端电机 B 解析：`id = 0x48A >> 7 = 9`，`cmd = 0x48A & 0x7F = 0x0A`
- 因为 `cmd = 0x0A < 0x80`，只能进普通命令分支，且节点 9 匹配 → `OnCanCmd(0x0A, ...)` 被调用
- 但 switch 里没有 `case 0x0A` 对应处理 → 静默丢弃

**实际影响**：
- 主控收不到任何 STALL 广播 → 不切换 RGB 状态，不触发 LOCKED 模式
- 其他电机收不到 STALL 广播 → 不会进入 LOCKED
- UNLOCKED 广播也收不到 → 无法解除 LOCKED

**根本原因**：

CAN StdId = `(nodeID << 7) | cmd`，共 11 位。当 nodeID 高位（bit 6）=1 时（如 nodeID=9=0b1001，bit6=1），
`cmd` 的 bit 6 会被 nodeID 的 bit 6 "覆盖"。解析时只取低 7 位 `StdId & 0x7F` 必然丢失信息。

**修复方案**：

将新命令码迁移至 0x50~0x7F 区间，并将电机端广播判断阈值从 `0x80~0xBF` 扩展为 `cmd >= 0x50`：

| 原命令码 | 新命令码 | 说明 |
|--------|---------|------|
| `0x8A` | `0x5A` | STALL/STALL_DONE/TRIGGER/DONE/TIMEOUT 广播 |
| `0x8B` | `0x5B` | UNLOCKED 广播 |
| `0x8C` | `0x5C` | 查询 stall 状态（主控单播） |

电机端 `can.c`：`if (cmd >= 0x80 && cmd <= 0xBF)` → `if (cmd >= 0x50)`

这样 `(nodeID << 7) | 0x5A` 解析后 `cmd = 0x5A >= 0x50`，进入广播分支。

**涉及文件**：
- 所有电机固件：`Core/Src/can.c`、`UserApp/main.cpp`（发送）、`UserApp/protocols/interface_can.cpp`（接收）
- 主控端：`Robot/actuators/ctrl_step/ctrl_step.cpp`（发送）、`UserApp/protocols/can_protocol.cpp`（接收）

---

### Bug-6：RETREATING 期间主控继续下发位置命令覆盖 retreatTarget（P0 Critical）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

电机触发堵转进入 RETREATING 后，主控的 MoveJ 阻塞循环因收不到 STALL 广播（Bug-5）而继续执行，
不断向该电机发送位置命令（0x05/0x07）。电机端 CAN 命令处理中仅检查 `stallMode == STALL_LOCKED` 才丢弃位置命令，
对 `stallMode == STALL_RETREATING` 不做拦截，导致 `SetPositionSetPoint` 被反复调用，
`retreatTarget` 被覆盖为原目标（如 251mm），电机停止回退并反向冲回物理限位。

**实测现象**：57 电机当前位置 240mm，目标 251mm → 触发堵转回退 → 回退后没有停止，又往 251mm 方向移动到物理限位。

**修复方案**：

在 `interface_can.cpp` 的 `case 0x05` 和 `case 0x07` 中，增加 `STALL_RETREATING` 丢弃条件：

```cpp
// LOCKED/RETREATING 状态丢弃位置命令（避免主控 stuck MoveJ 时反复下发覆盖 retreatTarget）
if (motor.controller->stallMode == Motor::STALL_LOCKED ||
    motor.controller->stallMode == Motor::STALL_RETREATING)
    break;
```

**涉及文件**：所有电机固件的 `UserApp/protocols/interface_can.cpp`

---

### Bug-7：RETREATING→LOCKED 转换依赖的 state 被 STATE_STALL 覆盖（P1）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

文档原写"复用 `state == STATE_FINISH` 判定 RETREATING→LOCKED 转换"，但实际实现中，
20kHz 控制循环的 `Update State` 段将 RETREATING/LOCKED 状态统一覆盖为 `STATE_STALL`，
导致 RETREATING handler 中的 `state == STATE_FINISH` 检测永远不成立，电机只能依赖 2s 超时才能转入 LOCKED。

**实测影响**：正常回退到位后电机不会主动切 LOCKED，必须等 2s 超时才锁定。

**修复方案**：

直接用原始变量判定，不依赖 `state`：

```cpp
bool reachedGoal = (controller->modeRunning == MODE_COMMAND_POSITION) &&
                   (controller->softPosition == controller->goalPosition) &&
                   (controller->softVelocity == 0);
if (reachedGoal)
{
    controller->stallMode = STALL_LOCKED;
    // ...
}
```

**涉及文件**：所有电机固件的 `Ctrl/Motor/motor.cpp`

---

### Bug-8：disable 路径未恢复 ratedVelocity（P1）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

电机进入 RETREATING 时，`motionPlanner.positionTracker.NewTask()` 将 `ratedVelocity` 临时改为 50000 step/s（用于慢速回退）。
enable 路径已恢复 `ratedVelocity`，但 disable 路径（0x01 RxData=0）未恢复。
虽然 disable 进入 `MODE_STOP` 后不会再跑速度相关逻辑，但 disable 再 enable 时可能拿到错误的 ratedVelocity。

**修复方案**：

disable 路径增加与 enable 路径一致的恢复逻辑：

```cpp
motor.config.motionParams.ratedVelocity = boardConfig.velocityLimit;
motor.motionPlanner.positionTracker.SetVelocityAcc(boardConfig.velocityAcc);
```

**涉及文件**：所有电机固件的 `UserApp/protocols/interface_can.cpp`

---

### Bug-9：enable/UNLOCKED 路径未重置 goalPosition，导致电机反复堵转（P0 Critical）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

实测发现：电机堵转 LOCKED 后，用户发送 `!STALL_UNLOCK` 或 enable → 电机重新冲回堵转前的目标位置 → 再次堵转 → 再次回退 → 死循环。

**根本原因**：

`interface_can.cpp` 的 enable 路径（`case 0x01`）和 UNLOCKED 路径（`case 0x5B`）虽然调用了 `motionPlanner.positionTracker.NewTask(estPosition, estVelocity)`，让 motionPlanner 起点设为当前位置。但 **`controller->goalPosition` 没被重置**，仍是堵转前的目标（如 251mm）。

下一个 20kHz 周期：
1. `CalcSoftGoal(goalPosition=251mm)` → `softPosition` 计算成 251mm
2. DCE 控制输出 → 驱动电机往 251mm 方向走
3. 抵达物理限位 → 电流飙高 + 误差大 + 速度低 → 再次触发堵转

**修复方案**：

新增 `Controller::ResetGoalsToCurrentPosition()` 公有方法封装清理逻辑：

```cpp
void Motor::Controller::ResetGoalsToCurrentPosition()
{
    SetPositionSetPoint(estPosition);  // goalPosition = estPosition
    goalVelocity = 0;
    goalCurrent = 0;
    softNewCurve = true;
}
```

在 enable/disable/UNLOCKED 三个分支调用 `ResetGoalsToCurrentPosition()`。

**修复后行为**：
- `goalPosition == estPosition` → `CalcSoftGoal()` 算出 `softPosition = estPosition`
- `estError = softPosition - estPosition = 0` → 电机"已到位"，不再主动运动
- 等待用户下个新命令

**涉及文件**：
- 所有电机固件：`Ctrl/Motor/motor.h`（新增方法声明）、`Ctrl/Motor/motor.cpp`（方法实现）
- 所有电机固件：`UserApp/protocols/interface_can.cpp`（enable/disable/UNLOCKED 分支调用）

---

### Bug-10：堵转检测触发时间不稳定，几百ms 到几秒（P1）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

实测发现堵转检测触发时间从 ~200ms 到几秒不等，极不稳定。

**根本原因**：

原检测算法使用"任意条件不满足即清零计时器"的策略，但 `estVelocity` 在堵转时高频抖动（步进电机微步振动、编码器量化噪声），导致：

1. 堵转时 `|estVelocity|` 在 ±10 step/s 周围频繁跳动
2. 任何一帧 `|estVelocity| > 10` → 计时立刻清零 → 重新从 0 开始
3. 触发时间被无限延长

此外，原速度阈值 10 step/s 太严格（实际堵转时电机小幅抖动到 10~20 step/s 是常态）。

**修复方案**：

采用方案 E：**上升沿 + 二次验证**：

1. **上升沿检测**：三条件首次同时满足时记录 `stallDetectRisingEdge=true`，记录此刻的 `|estError|`
2. **持续累加**：上升沿触发后，30ms 内三条件**任意条件不满足不再清零计时**，只检查电机是否真的动起来（`velocity > 150 step/s` 才清零）
3. **二次验证**：30ms 后再次判定：
   - 电流仍 > 阈值 60% + 误差仍 ≥ 上升沿时刻 80% → 触发
   - 否则继续累积，到 400ms 兜底强制触发
4. **阈值放宽**：
   - 速度阈值：`10 → 30 step/s`
   - 误差阈值：`50 → 30 step`（堵转压紧后误差可能缩小到 < 50）
5. **状态机增加字段**：
   - `stallDetectRisingEdge`：上升沿标志
   - `stallDetectRisingEstError`：上升沿时刻的 |estError|

**修复后预期**：
- 触发时间稳定在 30ms 左右（标准堵转场景）
- 阈值放宽避免小幅抖动误清零
- 二次验证避免"电机瞬间卡住又脱困"误触

**涉及文件**：
- 所有电机固件：`Ctrl/Motor/motor.h`（新增 2 个字段）
- 所有电机固件：`Ctrl/Motor/motor.cpp`（重写 20kHz 检测逻辑、Init 重置、softNewCurve 重置）

### Bug-11：主控 `SetStallMode()` 未同步 `targetRailPos`，enable 后地轨反复堵转（P0 Critical）

**发现日期**：2026-08-25
**修复状态**：✅ 已修复（2026-08-25）

**问题描述**：

地轨在 240mm，发送 251mm（物理上限 250mm）→ 57 电机正常触发堵转 → 回退到 245mm → LOCKED，RGB 切红色心跳。
用户发送 `!START`（enable）后，电机没有停在 245mm，而是又向 251mm 方向移动再次触发堵转，无限循环。

**根本原因**：

`dummy_robot.cpp` 的 `SetStallMode(int)` 只把 `targetJoints = currentJoints`（同步了 J1~J6），但**没有同步地轨**：

```cpp
// 修复前（错误）：
targetJoints = currentJoints;
for (int j = 1; j <= 6; j++) {
    motorJ[j]->targetAngle = currentJoints.a[j - 1] - initPose.a[j - 1];
}
// targetRailPos 仍是堵转前的 251mm！
```

主控后台主循环（`main.cpp` L41-L46）每 500ms 调一次 `MoveRail(targetRailPos)`：
- 堵转 LOCKED 期间 → 电机端 `case 0x07` 拦截 → 没问题
- `!START` 后电机进入 IDLE，`stallMode != LOCKED` → 不再拦截
- 主循环下一帧（≤500ms 内）下发 `0x07 SetPosition(251)` → 覆盖电机端 `ResetGoalsToCurrentPosition()` 设的 `goalPosition=245mm`
- 电机向 251 推进 → 抵达物理限位 → 再次堵转 → 死循环

电机端 Bug-9 的 `ResetGoalsToCurrentPosition()` 修复本身是对的，**遗漏在主控端没同步目标位置**。

**修复方案**：

在 `SetStallMode(int)` 中新增一行 `targetRailPos = currentRailPos`：

```cpp
void DummyRobot::SetStallMode(int motorIndex)
{
    SetRGBMode(RGB::RED_HEARTBEAT);
    targetJoints = currentJoints;
    for (int j = 1; j <= 6; j++) {
        motorJ[j]->targetAngle = currentJoints.a[j - 1] - initPose.a[j - 1];
    }
    // Bug-11 修复: 同步 targetRailPos 到 currentRailPos
    targetRailPos = currentRailPos;
    commandHandler.ClearFifo();
    (void)motorIndex;
}
```

**修复后行为**：
- 主循环 `MoveRail(targetRailPos=currentRailPos=245mm)` 下发到电机 → 电机 `goalPosition=245mm` → 与 `estPosition=245mm` 一致 → 不动
- 用户发新位置命令（如 `>200,...`）→ `MoveJ` 把 `targetRailPos=200` 下发 → 电机从 245 → 200 正常运动
- 与文档"场景 B 抛弃原目标"语义一致：堵转后抛弃原 251mm 目标，电机停在 245mm 等用户下个新命令

**涉及文件**：
- 主控：`firmware/ref_core_f405/Robot/instances/dummy_robot.cpp`（`SetStallMode(int)` 新增 1 行）

**已知遗留（不阻塞，TODO）**：
- `IsMoving()` 不监控地轨（`dummy_robot.cpp` L560-L569 只看 i=1..6）：MoveJ 阻塞循环在 STALL_DONE 后会立即看到 J1~J6 都到位而退出打印 "ok"，但地轨可能还在回退中。这不影响本次 Bug-11 修复，但会导致"主控提前返回 ok、地轨还在动"的体验问题。
- 修复方案待办：把地轨判定加入 `IsMoving()`，并让 `UpdateJointAngles()` 轮询地轨更新 `currentRailPos`（参考文档《重构方案—MoveJ速度与同步抵达—重构需求.md》）。

---

## §7 重构评估（2026-08-25）

**整体评分**：B+（80/100）

**三大亮点**：

1. **状态机架构清晰**：IDLE / RETREATING / LOCKED 三态转换显式定义，状态转移条件明确；废弃 `isStalled` 布尔和 `STATE_STALL` 状态，简化认知负担。
2. **二次验证防误触**：堵转检测用上升沿 + 二次验证（方案 E），避免瞬时卡顿误判，触发时间稳定在 ~30ms。
3. **目标位置同步机制**：`SetStallMode()` + `ResetGoalsToCurrentPosition()` 两端联动，强制 `targetAngle/targetRailPos = currentJoints/currentRailPos`，防止"enable 后再次冲回堵转点"的死循环。

**两大教训**：

1. **三套并行电机固件（35/42/57）需要纪律同步**：HEARTBEAT 语义在 57 固件被改动但未同步到主控，造成调试一周的低效沟通；未来任何协议级变更必须三套固件同步合入。
2. **"目标位置一定堵转"是已知设计取舍**（硬限位场景）：用户发 `>240,...` 让 J3 到 75°，但机械硬限位 60° 时，MoveJ 阻塞循环会静默返回 "ok"，但实际 J3 停在 60°。当前需要用户自行观察机械臂位置或 RGB 红光。详见 §8。

**整体结论**：重构达成核心目标（解决 4 个 P0 Critical Bug + 7 个 P1 Bug），架构层面已具备持续维护的清晰度，但用户体验层面的"堵转静默失败"尚未覆盖。

---

## §8 未来工作

### §8.1 当前已知但暂不修的项

| 优先级 | 项 | 不修原因 | 影响 |
|--------|---|---------|------|
| P1 | HEARTBEAT 副语义分离（P1-1） | 当前主控完全忽略 HEARTBEAT（仅识别 TRIGGER/DONE/TIMEOUT），无实际影响 | 无运行时影响；如未来需 250ms 周期同步，需重做主控侧状态机 |
| P1 | `IsMoving()` 监控地轨（P1-3） | 现场未遇到"地轨未到但程序返回 ok"现象 | 极端边界场景的体验问题，不影响功能 |
| P2 | PID 合并去重（P2-1，can_protocol.cpp 120→30 行） | 纯重构，行为完全等价 | 维护成本（每改一次 PID 代码要改 12 处） |
| P2 | `stallBroadcastCmd` 数据竞争保护（P2-4） | F103 单字节读写原子 + 事件稀疏（每 250ms 一次），实测未出问题 | 学术上"未定义行为"，工程上零影响 |

### §8.2 已知场景与应对（用户教育）

#### 场景："目标位置一定堵转"（机械硬限位）

**描述**：
- 用户发 `>240,...` 让 J3 目标角度 75°
- J3 实际物理硬限位 60°
- 电机触发堵转 → 回退 → LOCKED
- `SetStallMode` 同步 `targetAngle = currentJoints = 60°`
- `IsMoving()` 返回 false → MoveJ 阻塞循环返回 "ok"
- 用户以为命令成功，实际 J3 停在 60°
- 用户重发同一目标 → 又堵转 → 又返回 "ok"，循环

**当前应对**：
- 用户自行观察机械臂实际位置
- 用户观察 RGB 红色心跳
- 用户读取 `#GETJPOS` 主动查询

**未实现**（用户可见警告）：
- 没有 `[STALL] node=3 locked` 之类的 ASCII 输出到 Python 串口助手
- 串口助手无法识别"静默失败"

**设计决策**（已记录）：
- 不修 Bug（避免和"设全局目标让用户重发"的设计冲突）
- 仅文档登记，等用户教育成熟或串口助手需要支持时再实现
