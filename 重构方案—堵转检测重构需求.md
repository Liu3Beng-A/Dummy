# 堵转检测重构需求

> **文档目的**：把堵转检测重构的所有决策与实施要点固化下来，作为后续固件重构的依据。
> **文档状态**：v2.0 重写版（2026-08-23），从原 4950 行精简到约 800 行。
> **2026-08-23 24:00 增量**：电流阈值改为 `ratedCurrent × 75%`（详见 1.7），替换 1.5 旧绝对值决策。
> **核心决策冻结**：回退距离、状态机、ENABLE/DISABLE 解除语义已冻结，不再讨论。

---

## 0. 重构目的（用户最新原话，2026-08-23）

### 0.1 功能目标

实现**可用的堵转检测**：


| 项                   | 内容                                                                                           |
| ------------------- | -------------------------------------------------------------------------------------------- |
| 触发条件                | 电机持续堵转 200ms（D4 三元组判定）                                                                       |
| 电流阈值（isPowering 判据） | **自适应**：`motionParams.ratedCurrent × D4_IS_POWERING_RATIO / 100`，`D4_IS_POWERING_RATIO = 75` |
|                     | `motionParams.ratedCurrent` = EEPROM `boardConfig.currentLimit`，CAN 0x12 可运行时调               |
|                     | 默认场景：35=1500mA→1125mA / 42=2000mA→1500mA / 57=2500mA→1875mA                                  |
|                     | 1A 限幅场景：57 限幅 1000mA → 阈值 750mA（堵转电流被限幅卡在 1A > 750mA 仍能触发）                                   |
| 关节回退                | j1~j6 回退 **5°**；j6 因减速比 30:1（不是 50:1），实际回退角度更大，**允许多回退一些**                                   |
| 地轨回退                | 57 电机（地轨）回退 **5mm**（丝杠 1605 直驱 1 圈）                                                          |
| 回退完成                | 保存**当前姿态**、更新**角度**、电机进入 LOCKED 状态                                                           |
| 视觉反馈                | 触发堵转后 RGB 切换为**心跳模式**                                                                        |
| 解除方式                | 发送 **enable**（保持当前姿态）或 **disable**（失能电机）解除堵转模式                                               |


### 0.2 关键事实（实测）

```
1mm = 10240 步（丝杠 1605 直驱，51200 步/圈 ÷ 5mm 导程）
5mm = 51200 步（57 电机 1 圈）  ← 用户实测：发送 5mm 移动 = 转 1 圈 ✓
关节减速比：j1~j5 = 50:1，j6 = 30:1
```

### 0.3 回退距离核算


| 电机                  | 回退目标   | 计算                                | 步数          |
| ------------------- | ------ | --------------------------------- | ----------- |
| j1~j5（35/42电机，50:1） | 输出轴 5° | 51200 × 50 × 5° / 360° = 35555.56 | **35556 步** |
| j6（35电机，30:1）       | 输出轴 5° | 51200 × 30 × 5° / 360° = 21333.33 | **21333 步** |
| 地轨（57电机直驱）          | 丝杠 5mm | 51200 × 5mm / 5mm = 51200         | **51200 步** |


> ⚠️ **当前代码错误**：35/42 main.cpp 默认 `retreatSteps = 711`（× 50 才对），57 main.cpp 默认 `retreatSteps = 204800`（× 25 才对，**注意**：用户实测 57 = 51200 步已经是 1 圈，所以原 204800 = 4 圈是错的）。

### 0.4 不重构的范围

- 夹爪固件（CAN ID=8）**不参与**堵转检测（抓取物体本身就是堵转，无法区分）
- 主控串口协议、ASCII 命令、CAN 协议格式**不动**
- 主控 `RefactoredCmdRobot` 业务逻辑**不动**
- FreeRTOS 任务划分**不动**

---

> 🔴 **2026-08-23 19:00 重构现状标记（重要）**
>
> **本轮重构必须采用 D4 方案（第 17 章），不能回到文档 #44 旧"电流+速度+位置误差三元组"。**
>
> 理由：
>
> - 2026-08-23 16:30~~17:30 用户实测发现 commit `365748f` 中"threshold = ratedCurrent × 95%" 方案对 1A 限流场景失效（详见第 13~~15 章根因分析）
> - 文档 #44 的绝对电流阈值（35=900/42=1100/57=1600 mA）同样会被用户改 ratedCurrent 废掉
> - 唯一自适应方案：D4（"控制意图 vs 物理响应不匹配"判定，含 3 个高风险修复）
>
> 🟢 **2026-08-23 24:00 增量**：电流阈值决策更新（详见 1.7）
>
> - 实测确认 `motionParams.ratedCurrent × D4_IS_POWERING_RATIO / 100` 方案（k=75）能处理 1A 限幅场景
> - 替换旧决策（绝对 900/1100/1600 mA），文档偏差表已新增 偏差-29 持续维护
>
> 🟡 **2026-08-24 00:40 增量**：LOCKED 入口 ClearIntegral + RETREATING→LOCKED 阈值决策更新（用户拍板）
>
> - **Q1 决策 A1**（用户拍板，替代早期讨论的 A2 草案）：LOCKED 入口**一次性** `ClearIntegral()`（清 RETREATING 期间累积的脏 I 项，避免 LOCKED 第一帧爆冲），随后靠 P 项 + DCE 重积分自动输出保位力矩；解除 stall（IDLE）再次 ClearIntegral 防爆冲
> - **Q2 决策**（用户拍板）：RETREATING→LOCKED 触发阈值由 `MOTOR_ONE_CIRCLE_SUBDIVIDE_STEPS / 10 = 5120` 步改为 **5 步**（文档偏差-28 要求）。超时兜底 `STALL_RETREAT_TICKS = 2000ms` 不变
> - **影响文件**：`motor.cpp:355-356`（3 个电机目录），`Ctrl/Motor/motor.cpp`（60 项 #5 LOCKED 入口补 ClearIntegral）
>
> **当前代码与文档偏差**（2026-08-23 19:00 盘点）：
>
>
> | #     | 文档要求                                                                        | 当前代码                                                                     | 偏差                                           |
> | ----- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------- |
> | 偏差-1  | 文档 D4（pError/goalCurrent + estVelocity + focCurrent > 75% rated）200ms 累加    | motor.cpp L288-340 D4 三元组已实施；k=75% 已落实                                   | **合规**                                       |
> | 偏差-2  | 文档 17.2.1：Encoder 健康 + 启动豁免期 + 力矩模式分支                                       | Encoder 健康已加；启动豁免期常量定义但未调用；力矩模式分支已加                                      | 缺启动豁免期逻辑                                     |
> | 偏差-3  | （旧决策已废）35=900/42=1100/57=1600 电流阈值                                          | 不再使用电流绝对值；走 D4 focCurrent > 75% rated                                    | **旧决策作废**，k=75% 已固化（1.7 决策）                  |
> | 偏差-4  | 文档 #44：35/42 回退 35556 步 / 57 回退 51200 步                                     | main.cpp 默认 35/42=711 步 / 57=204800 步                                    | **错值**                                       |
> | 偏差-5  | 文档 Q8：0x89 急停清 stallMode                                                    | interface_can.cpp L323-333 0x89 已调 ClearStallFlag()                      | **合规**                                       |
> | 偏差-6  | 文档 #29：删除 95% overload 检测                                                   | motor.cpp 不存在 overload 检测                                                | **合规**                                       |
> | 偏差-7  | 文档 #57：softNewCurve 路径不在 LOCKED 时清 stall                                    | motor.cpp L195-197 注释明确不再无条件 ClearStallFlag                              | **合规**                                       |
> | 偏差-8  | 文档 #42：**保留** ClearStallFlag（不删除）                                           | motor.cpp:608 + interface_can.cpp L42,L331 + main.cpp:215 均使用            | **合规**                                       |
> | 偏差-9  | 文档 2.5.6.1 注释 ①：所有关节电机统一 35556 步                                            | main.cpp 35/42=711 步                                                     | **与文档不一致**                                   |
> | 偏差-10 | 文档 #44：LOCKED 状态 CalcDceToOutput（PID 保位）                                    | motor.cpp L119-124 LOCKED 分支已实施；**未做入口 ClearIntegral**                   | 缺 LOCKED 入口清积分（Q1 用户拍板 A1：补 ClearIntegral） |
> | 偏差-11 | 文档 #19/#39：夹爪固件不参与 stall 检测                                                 | gripper 干净                                                               | **无偏差**                                      |
> | 偏差-12 | 文档 #22：主控 MoveJ/MoveL 拦截 stall mode                                         | ascii_protocol.cpp L791-796 "send !STALL_RESUME first"                   | 文档原文 "send !START first"；实际 STALL_RESUME 更准确 |
> | 偏差-13 | 文档 #25：`!STALL_STATUS` 响应 `>stall: j1=IDLE ...`                             | dummy_robot.cpp:564 输出 `ok STALL_STATUS rail_en=...`                     | 格式与文档不一致                                     |
> | 偏差-14 | 文档 #47：`CtrlStepMotor::StallMode_t` + `SetAngle` 拦截                         | ctrl_step.hpp 缺                                                          | 主控 LOCKED 时仍发位置命令到电机端（被电机端拦截，但浪费 CAN）        |
> | 偏差-15 | 文档 #58：encoder 校准时强制 stallMode=IDLE                                         | interface_can.cpp 0x02 仅设 isTriggered                                    | 校准可能被 stall 干扰                               |
> | 偏差-16 | 文档 L5/L8：0x01 enable=true LOCKED 时 softNewCurve=true 切回 POSITION            | interface_can.cpp L38-39 仅切 VELOCITY，未设 softNewCurve                     | LOCKED 后无法恢复                                 |
> | 偏差-17 | 文档 L6：0x01 enable=false LOCKED 时 stallMode=IDLE                             | interface_can.cpp enable=false 不调 ClearStallFlag                         | disable 后 LOCKED 残留                          |
> | 偏差-18 | 文档 L7：RETREATING 期间重启 motion planner                                        | motor.cpp L348 每周期 SetPositionSetPoint，未设 softNewCurve                   | retreatTarget 不生效                            |
> | 偏差-19 | 文档 #44 LOCKED 入口 ClearIntegral                                              | 偏差-10 重述（Q1 用户拍板 A1）                                                    | ⏳ 待实施                                       |
> | 偏差-20 | 文档 Q6：ENABLE 0x01 后 goalVelocity=estVelocity                                | interface_can.cpp L38-39 切 VELOCITY 但未调 SetVelocitySetPoint(estVelocity) | 启动减速冲击                                       |
> | 偏差-21 | 文档 #47/#54：主控 CtrlStepMotor 接收 0x7C 更新 stallMode                            | ctrl_step 无 0x7C listener                                                | 偏差-14 根因                                     |
> | 偏差-22 | 文档 C2/C5：MoveJ/Homing/Resting 阻塞循环 stall 检查 + 10s 超时                        | dummy_robot.cpp:507 `while (IsMoving())` 死循环                             | stall mode 下死锁                               |
> | 偏差-23 | 文档 C3：SetEnable(false) 清 motorStallMask                                     | dummy_robot.cpp:534 disable 分支不清                                         | disable 后 mask 残留                            |
> | 偏差-24 | 文档 #50：ClearStallFlag 时上报 0x7C IDLE                                         | motor.cpp:608 ClearStallFlag 不发 0x7C                                     | 主控 mask 不自动更新                                |
> | 偏差-25 | 文档 #22/#57：encoder_calibrator 入口强制 stallMode=IDLE                           | encoder_calibrator 入口未强制清                                                | 校准时 stall 干扰                                 |
> | 偏差-26 | 文档 #51/#44：电机端 LOCKED 分支入口 ClearIntegral                                    | 偏差-10/19 重述（Q1 用户拍板 A1）                                                 | ⏳ 待实施                                       |
> | 偏差-27 | 文档 17.2.1（修复B）：enableTimestamp + 启动豁免期判定                                    | 无 enableTimestamp 字段                                                     | 启动期可能误触                                      |
> | 偏差-28 | 文档 #18：RETREATING→LOCKED 阈值 5 步（误差 < 5 步）                                   | motor.cpp L353-354 用 MOTOR_ONE_CIRCLE_SUBDIVIDE_STEPS/10 = 5120 步        | 回退触发过宽（Q2 用户拍板：改为 5 步）                  |
> | 偏差-29 | （2026-08-23 24:00 增量）电流阈值从绝对 mA（900/1100/1600）改为 `ratedCurrent × 75%`（k=75） | 决策已落实（1.7 节）；电机端 motor.cpp 已实施 k=75%                                     | **已闭环**                                      |
>
>
> **本轮重构目标**：按 D4 方案（第 14/17 章）+ LOCKED 状态机（第 1~~12 章）严格实施，修复偏差 2/4/9/10/13~~27（共 17 项）+ 新增偏差 29 持续维护。偏差 1/3/5/6/7/8/11/12/29 已合规。

---

## 📑 文档结构（7 章 + 偏差表）


| #   | 章节            | 主题                                                          |
| --- | ------------- | ----------------------------------------------------------- |
| 0   | 重构目的          | 用户最新原话（5°/5mm 回退、enable/disable 解除、RGB 心跳）                  |
| 1   | 用户原话决策时间线     | 23:19 / 23:45 / 23:51 / 23:57 / 04:44 / 16:30~17:30 / 19:00 |
| 2   | 核心算法与状态机      | D4 三元组 + IDLE/RETREATING/LOCKED 状态机 + LOCKED PID 保位 + 启动豁免期 |
| 3   | 参数与硬编码常量      | 回退距离、电流阈值、速度、20kHz 时间基准                                     |
| 4   | 按文件分组的修改清单    | 电机端 35/42/57/gripper + 主控 5 个文件的精确改动点                       |
| 5   | 用户 60 项决策核对清单 | 冻结决策，不再讨论                                                   |
| 6   | 测试与验证清单       | 重构后必验证项 + 文档结尾快速回归                                          |
| 7   | 当前偏差表         | 28 项偏差，标记当前代码与文档要求的差距                                       |


---

> - **回退完成判定**：写死回退距离（5° 或 5mm），不需要"判定回退完成"事件钩子——电机走到目标位置自然停，改用 20kHz **周期性查位置误差**判定（误差 < 5 步切 LOCKED）
> - **退出 stall 方式**：**删除 0x1D STALL_RESET CAN 命令 和 !STALL_RESET ASCII 命令**——改用现有 `0x01 enable` / `0x02 disable` 在 LOCKED 状态下**自动清除 stallMode**。用户用 `!START` / `enable` / `!DISABLE` 任一命令可解除堵转
>
> ⚠️ **2026-08-21 23:45 用户决策（已确认 Q1~Q5，需求冻结）**：
>
> - **回退距离**：j1~~j6 关节回退 **5°**（J1~~J5: 35556 步，J6: 21333 步）；57 电机（地轨）回退 **5mm**（51200 步，丝杆 1605 直连 1 圈 = 5mm）
> - **触发延迟**：全部 **200ms**
> - **电流阈值**：35=900mA / 42=1100mA / 57=1600mA（按 2026-08-21 23:51 用户降低决定——保守下限避免误触）
> - **预存 Bug B3/B4**：暂不修复，等开始重构时再处理
>
> ⚠️ **2026-08-21 23:20 用户决策（推翻原三层方案）**：
>
> - 撤掉"自动恢复 / 三层防护 / 降功率保留控制"三件套
> - 改用**单层检测 + 回退 + 锁死 + 手动解除**语义
> - 触发堵转 → 电机按预设距离/角度**反向回退** → **直接落到目标位置后停止**（无需判定回退完成）
> - 落点后电机进入 **STALL_LOCKED** 状态：不再响应任何位置命令
> - **退出 stall 不再需要专用命令**：`!START` / `enable` / `!DISABLE` 三选一都可以解除堵转状态
> - 解除后电机保持在回退后的状态，**等待下一个运动指令**
> - 检测算法要求：**识别准确**，**运算开销低**（不能在 20kHz 闭环里堆算）

---

## 1. 用户原话决策时间线

按时间倒序排列，**所有决策一旦写下不再改动**。

### 1.1 2026-08-23 19:00 — 重构目的冻结

> **实现可用的堵转检测；检测到堵转的电机回退；j1-j6 回退 5°（j6 因减速比 30 允许多回退一些）；地轨电机回退 5mm；回退完保存当前姿态、更新角度；触发堵转后 RGB 切换到心跳；发送 enable 或 disable 时解除堵转模式，enable 后保持当前姿态，disable 后失能电机。**

### 1.2 2026-08-23 17:30 — D4 增强方案最终版

- D4 = "控制意图 vs 物理响应不匹配"判定，公式：
  - `commandingMotion = (|Δtarget| > 0) && (target 在推进)`
  - `actuallyStopped = (|estVelocity| < MIN_SPEED) && (|pError| > STALL_ERROR_THRESH)`
  - `isPowering = |focCurrent| > motionParams.ratedCurrent × D4_IS_POWERING_RATIO / 100`（自适应 EEPROM 限幅，k=75）
  - **触发条件：`commandingMotion && actuallyStopped && isPowering` 持续 200ms**
- 三元组齐全才触发，**避免单一判据的误判**
- 检测时间窗口 200ms（4000 ticks @ 50us tick）

### 1.3 2026-08-23 04:44 — Q6~Q10 全部确认

- **Q6**: ENABLE 0x01 后 `goalVelocity = estVelocity`，避免减速冲击
- **Q7**: LOCKED 状态一次性 ClearIntegral 后**保留** PID I 项（保位力矩）
  - **2026-08-24 00:40 决策确认 A1**（替换早期 A2 草案）：LOCKED 入口**先一次性 `ClearIntegral()`** 清 RETREATING 期间累积的脏 I 项（避免 LOCKED 第一帧 PID 输出爆冲），随后靠 P 项 + DCE 重积分自动输出保位力矩
  - 三个时间点分别清：RETREATING 入口（#30 决策 4：保留脏 I 项给 RETREATING 自身用）→ LOCKED 入口（本条：清脏 I 项）→ 解除 stall IDLE（#32 决策 32：清 LOCKED 期间新累积的 I 项防爆冲）
- **Q8**: 0x89 急停清 stallMode（覆盖 LOCKED/RETREATING）
- **Q9**: RETREATING 期间拒绝 0x03~0x07，仅 0x01/0x02/0x89 可处理
- **Q10**: `lastMoveDirection` 初始 +1（无副作用）

### 1.4 2026-08-21 23:57 — 删除冗余命令

- 删除 `0x1D STALL_RESET` 命令码
- 删除 `!STALL_RESET` ASCII 命令
- 解除堵转统一走 `enable`（保持姿态）或 `disable`（失能电机）

### 1.5 2026-08-21 23:51 — ~~降低电流阈值（已废止，见 1.7）~~

> ⚠️ **本节决策已废止**——用户实测发现绝对电流阈值（35=900 / 42=1100 / 57=1600 mA）会被 `boardConfig.currentLimit` 调整废掉（用户把 57 限幅改成 1A 后 1.6A 阈值永远到不了）。详见 1.7 用比例替换。

```
旧决策（不再使用）：
  35（关节 j4~j6）| 900 mA
  42（关节 j1~j3）| 1100 mA
  57（地轨） | 1600 mA
```

### 1.6 2026-08-21 23:19 — 推翻三层方案，确立回退锁死语义

> 触发堵转 → 电机按预设距离**反向回退** → 直接落到目标位置后停止（无需判定回退完成）→ 进入 STALL_LOCKED 状态（不再响应位置命令）→ 通过 `enable` / `disable` / `!START` 手动解除

### 1.7 2026-08-23 24:00 — 电流阈值改为「额定 × 比例」（替换 1.5 旧决策）

> 用户实测场景：57 地轨 `boardConfig.currentLimit` 可调（CAN 0x12）。当用户把 57 限幅改成 1A 时，旧的 1600 mA 绝对阈值永远到不了 → 堵转漏检。
>
> 解决：用 `**motionParams.ratedCurrent × D4_IS_POWERING_RATIO / 100`** 作为阈值。`ratedCurrent` 运行时从 EEPROM 读，自适应限幅。

```cpp
// 实施于 motor.cpp（35/42/57 同步）
static constexpr int32_t D4_IS_POWERING_RATIO = 75;  // 75%

bool isPowering = (abs(controller->focCurrent)
                  > (int32_t)(config.motionParams.ratedCurrent * D4_IS_POWERING_RATIO / 100));
```


| 电机          | 默认 ratedCurrent（EEPROM） | 阈值 = rated × 75%                     |
| ----------- | ----------------------- | ------------------------------------ |
| 35（j4~j6）   | 1500 mA                 | **1125 mA**                          |
| 42（j1~j3）   | 2000 mA                 | **1500 mA**                          |
| 57（地轨）      | 2500 mA                 | **1875 mA**                          |
| 57（1A 限幅场景） | 1000 mA（用户改）            | **750 mA**（堵转电流被限幅卡在 1A > 750mA 仍触发） |


**关键链路**：

```
hardware采样范围 3300mA（固定，不可改）
   ↑
   │ CalcCurrentToOutput 限幅
   │
 motionParams.ratedCurrent（运行时变量，来自 EEPROM）
   ↑                      ↑ ↑ ↑
   │ 启动时赋值            │ │ │
   │                      │ │ └── D4_IS_POWERING_RATIO = 75（编译时常量）
   │                      │ └──── isPowering 阈值 = rated × 75 / 100
   │                      └────── focCurrent（实时输出，被 rated 限幅）
 boardConfig.currentLimit（EEPROM，可通过 CAN 0x12 修改）
```

**重写后字段对应**：

- `config.motionParams.ratedCurrent` ← `boardConfig.currentLimit`（不变，已经在用）
- `D4_IS_POWERING_RATIO = 75` ← 替换旧 `STALL_CURRENT_THRESH_`*（3 个绝对值常量）

---

## 2. 核心算法与状态机

### 2.1 D4 三元组判定

```c
// === D4 三元组（电机端 CloseLoopControlTick 内执行）===
static constexpr int32_t D4_PERROR_THRESHOLD      = 800;     // 步（≈1.5625°）
static constexpr int32_t D4_IS_POWERING_RATIO     = 75;      // 通电判定：|focCurrent| > rated × 75/100
static constexpr int32_t D4_COMMANDING_CUR_RATIO  = 10;      // 力矩模式：|goalCurrent| > rated × 10/100
static constexpr int32_t D4_STARTUP_GRACE_MS      = 500;     // 启动豁免期（实测后调整）
static constexpr int32_t D4_VELOCITY_THRESHOLD    = (200 * 256) / 5;  // 1/5 圈/周期 = 10240 步

bool commandingMotion = (mode != MODE_STOP)
                     && (|Δtarget| > 0)             // 目标在推进
                     && (|estError| > D4_PERROR_THRESHOLD);  // 位置误差 > 阈值（"有控制意图"）
bool actuallyStopped = (|estVelocity| < D4_VELOCITY_THRESHOLD);  // 速度 < 1/5 圈/周期
bool isPowering = |focCurrent| > motionParams.ratedCurrent × D4_IS_POWERING_RATIO / 100;
                 // ↑ 自适应 EEPROM 限幅（不写死 mA）
bool isStalled = commandingMotion && actuallyStopped && isPowering;  // 三元组齐全
```

**关键点**：

- `D4_IS_POWERING_RATIO = 75`（75%，不是绝对 mA）→ 自适应 `boardConfig.currentLimit`
- 默认阈值：35=1125mA / 42=1500mA / 57=1875mA（ratedCurrent 默认 1500/2000/2500 mA）
- 1A 限幅场景：57 阈值自动降到 750 mA
- 三元组**必须同时满足**，缺失任一项都不触发

### 2.2 状态机

```
                ┌──────────────────────────────────────────┐
                │                                          │
                ▼                                          │
   ┌──────┐  触发  ┌────────────┐  回退到位  ┌──────────┐  │
   │ IDLE │ ──────▶│ RETREATING │ ─────────▶ │  LOCKED  │  │
   └──────┘        └────────────┘            └──────────┘  │
      ▲                                            │      │
      │                                            │      │
      │           ┌─────────────────────┐          │      │
      └───────────│  enable  │  disable │◀─────────┘      │
                  │ (保姿态) │ (失能)  │                  │
                  └─────────────────────┘                  │
                          ▲                              │
                          │  0x89 急停 / 0x02 disable 清 stallMode │
                          └──────────────────────────────────┘
```

**三种状态**：


| 状态         | 含义  | 关键行为                                                                         |
| ---------- | --- | ---------------------------------------------------------------------------- |
| IDLE       | 正常  | 接受位置命令、电流命令、速度命令                                                             |
| RETREATING | 回退中 | **拒绝** 0x03~0x07 位置/速度/电流命令；仅接受 0x01/0x02/0x89                               |
| LOCKED     | 锁死  | 电机保持当前位置（PID 持续输出保位力矩）；**拒绝所有运动命令**；保持 stallMode=LOCKED 直到 enable/disable 解除 |


### 2.3 状态机时序

```
T=0          触发堵转 → stallMode=RETREATING，启动 stallRetreatSteps
T=0~2ms     计算回退目标 (currentPosition - lastMoveDirection × retreatSteps)
T=2ms       调 SetPositionSetPoint() + softNewCurve=true
T=2~250ms  按 velocityLimit/velocityAcc 速度曲线回退（实测 ~0.236s）
T=250ms     motion_planner 到位（|realPos-retreatTarget| < 5 步）→ stallMode=LOCKED
            ClearIntegral() 一次性清 PID I 项
            发 0x7C `data[1]=LOCKED` 上报主控
            RGB 切心跳
T=∞         等用户发 enable（保姿态）或 disable（失能）

> 注：RETREATING→LOCKED 触发条件为 `|realPos - retreatTarget| < 5 步`（2026-08-24 Q2 决策，替代原 5120 步 = 1/10 圈容差）；超时兜底 `STALL_RETREAT_TICKS = 2000ms` 不变
```

### 2.4 LOCKED 状态 PID 保位机制

**问题**：重力负载下，电机停止输出会让关节往下掉。

**解决**：

- LOCKED 入口**一次性** `ClearIntegral()` 清 PID I 项（清 RETREATING 期间累积的脏 I 项，避免 LOCKED 第一帧 PID 输出爆冲）
- PID 持续运行（保 P 项 + 重新累积的 I 项），自动输出保位力矩
- 解除 stall 时（IDLE）再次 `ClearIntegral()` 防爆冲（清 LOCKED 期间累积的新 I 项）
- RETREATING 期间**不**清 I 项（电机正在回退，I 项累积是正常的，#4 决策）

> **2026-08-24 00:40 重申**：入口 ClearIntegral 必须在 LOCKED **状态切换那一帧**调用（不是每帧都清）。参见 motor.cpp LOCKED 分支（Q1 决策 A1 实施位置）。

### 2.5 启动豁免期

**问题**：电机刚 enable 时 PID I 项为空、电流/速度未稳定，立刻做堵转判定会误触发。

**解决**：

- ENABLE 0x01 入口记录 `enableTimestamp = millis()`
- 在 stall 判定前加：`if (millis() - enableTimestamp < 100ms) return false;`
- 100ms 内不进入堵转判定（关节 + 地轨统一）

### 2.6 边界条件


| 触发源                                   | 行为                                                                                                                                    |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `0x89 急停`                             | 立即 `stallMode = IDLE + ClearIntegral + MODE_STOP + Brake`（覆盖 LOCKED/RETREATING）                                                       |
| `0x02 disable`                        | 清 `stallMode = IDLE + Sleep`（电机失能不保位）                                                                                                 |
| `0x01 enable=true`                    | **LOCKED 状态**：`ClearStallFlag()` + 切回 POSITION 模式 + `softNewCurve=true` + `goalVelocity=estVelocity`；**其他状态**保留 `ClearStallFlag()` 行为 |
| `softNewCurve=true` 路径                | **只在 stallMode==IDLE 时**清 stall；LOCKED 时切模式不清 stall（防误清）                                                                              |
| `encoder_calibrator.isTriggered=true` | 强制 `stallMode = IDLE`（校准时关闭堵转检测）                                                                                                      |


---

## 3. 参数与硬编码常量

### 3.1 回退距离


| 电机    | 减速比  | 回退目标   | 步数          | 实际回退角度                |
| ----- | ---- | ------ | ----------- | --------------------- |
| j1~j5 | 50:1 | 输出轴 5° | **35556 步** | 5.00°                 |
| j6    | 30:1 | 输出轴 5° | **21333 步** | 5.00°（电机端 8.33°，用户接受） |
| 地轨    | 直驱   | 丝杠 5mm | **51200 步** | 5.00mm（57 电机 1 圈）     |


### 3.2 电流阈值（自适应 = ratedCurrent × D4_IS_POWERING_RATIO / 100）

> **2026-08-23 24:00 决策**：放弃 1.5 节的绝对 mA 阈值（35=900 / 42=1100 / 57=1600 mA），改用 `ratedCurrent × k` 比例方案（详见 1.7）。`ratedCurrent` 来自 EEPROM `boardConfig.currentLimit`，CAN 0x12 可运行时调。

**核心常量**：

```cpp
static constexpr int32_t D4_IS_POWERING_RATIO = 75;  // 75%（编译时常量，不进 EEPROM）
```

**各电机默认 ratedCurrent × 75%**：


| 电机        | boardConfig.currentLimit（默认） | isPowering 阈值（= rated × 75 / 100） |
| --------- | ---------------------------- | --------------------------------- |
| 35（j4~j6） | 1500 mA                      | **1125 mA**                       |
| 42（j1~j3） | 2000 mA                      | **1500 mA**                       |
| 57（地轨）    | 2500 mA                      | **1875 mA**                       |


**1A 限幅场景核算**（用户实测触发条件）：


| 场景        | boardConfig.currentLimit | 阈值（k=75%）  | 堵转 focCurrent | isPowering    |
| --------- | ------------------------ | ---------- | ------------- | ------------- |
| 57 默认     | 2500 mA                  | 1875 mA    | 被限幅卡在 2500 mA | ✓             |
| 57 用户改 1A | **1000 mA**              | **750 mA** | 被限幅卡在 1000 mA | ✓（1000 > 750） |
| 35 默认     | 1500 mA                  | 1125 mA    | 1500 mA       | ✓             |


**配套力矩模式判据**（用于 `MODE_COMMAND_CURRENT`）：

```cpp
static constexpr int32_t D4_COMMANDING_CUR_RATIO = 10;  // |goalCurrent| > rated × 10/100 视为"有意施加力"
```

### 3.3 回退速度


| 电机    | velocityLimit             | velocityAcc              | 实测回退时间  |
| ----- | ------------------------- | ------------------------ | ------- |
| 35/42 | 30×51200 = 1536000 步/s    | 100×51200 = 5120000 步/s² | ~0.236s |
| 57    | 600 步/200us = 3000000 步/s | 同上                       | ~0.017s |


### 3.4 时间基准

```
CONTROL_FREQUENCY = 20000          // 20 kHz
CONTROL_PERIOD    = 50 us          // tick 周期
STALL_DETECT_TICKS = 4000          // 200ms @ 50us tick
STALL_RETREAT_TICKS = 40000        // 2000ms（兜底超时：2s 内回退不到位则强制 LOCKED）
STALL_ENABLE_EXEMPTION_MS = 100    // 启动豁免期
```

### 3.5 错误阈值


| 参数                   | 值          | 说明     |
| -------------------- | ---------- | ------ |
| `MIN_SPEED`          | 30 步/200us | 速度判据下限 |
| `STALL_ERROR_THRESH` | 200 步      | 位置误差下限 |


---

## 4. 按文件分组的修改清单

### 4.1 电机端 motor.cpp（35/42/57 同步 3 份，gripper 不动）

**位置**：35/42/57 各 `Ctrl/Motor/motor.cpp`

**改动点**：


| #   | 位置                   | 内容                                                                  |
| --- | -------------------- | ------------------------------------------------------------------- |
| 1   | D4 三元组判定             | 替换原 `isStalled` 单一电流判据                                              |
| 2   | 状态机                  | `stallState.stallMode` 改为 `enum { IDLE=0, RETREATING=1, LOCKED=2 }` |
| 3   | CloseLoopControlTick | `STALL_DETECT_TICKS = 4000` 时间窗                                     |
| 4   | RETREATING 入口        | 一次性 ClearIntegral + softNewCurve=true + SetPositionSetPoint         |
| 5   | LOCKED 入口            | 一次性 ClearIntegral + 发 0x7C `data[1]=LOCKED`（Q1 A1 待实施）             |
| 6   | 0x89 急停              | 清 stallMode + ClearIntegral + MODE_STOP + Brake                     |
| 7   | 0x03~0x07 处理         | RETREATING 时 break（拒绝）                                              |
| 8   | 启动豁免                 | `if (millis() - enableTimestamp < 100) return false;`               |
| 9   | softNewCurve 路径      | **只在 stallMode==IDLE 时**调 `ClearStallFlag()`                        |


**保留**：`ClearStallFlag()` 函数（用户 23:57 决定）

### 4.2 电机端 interface_can.cpp（35/42/57 同步 3 份）


| CAN 命令                | 处理                                                                                                                     |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **0x01 enable=true**  | LOCKED → ClearStallFlag + 切 POSITION + softNewCurve=true + `SetVelocitySetPoint(estVelocity)`；其他状态保留 ClearStallFlag 行为 |
| **0x01 enable=false** | 仅清 stallMode（不强制 Sleep）；Sleep 由主控 SetEnable(false) 联动                                                                  |
| **0x02 disable**      | 同上（仅清 stallMode）                                                                                                       |
| **0x89 急停**           | 清 stallMode + ClearIntegral + MODE_STOP + Brake（已在 motor.cpp 处理）                                                       |
| **0x03~0x07**         | RETREATING 时直接 break；LOCKED 时直接 break                                                                                  |


### 4.3 电机端 configurations.h（35/42/57 同步）

> **2026-08-23 24:00 决策**：取消独立的 `currentThresh_mA` 字段。改用**编译时常量 `D4_IS_POWERING_RATIO = 75`**，运行时阈值 = `motionParams.ratedCurrent × 75 / 100`（详见 1.7/3.2）。
>
> 不需要新 EEPROM 字段、不需要新 CAN 命令（**符合 60 项清单 #42 极简原则**）。

**已存在的字段**（不需要改）：

- `motionParams.ratedCurrent` ← `boardConfig.currentLimit`（EEPROM，CAN 0x12 可调）

**新增常量**（写入 motor.cpp 顶部，**不进 EEPROM**）：

```cpp
// 编译时常量（35/42/57 同步）
static constexpr int32_t D4_IS_POWERING_RATIO     = 75;  // isPowering: |focCurrent| > rated × 75/100
static constexpr int32_t D4_COMMANDING_CUR_RATIO  = 10;  // 力矩模式: |goalCurrent| > rated × 10/100
static constexpr int32_t D4_PERROR_THRESHOLD      = 800; // 位置模式: estError > 800 步
static constexpr int32_t D4_VELOCITY_THRESHOLD    = (200 * 256) / 5;  // 1/5 圈/周期
static constexpr int32_t D4_STARTUP_GRACE_MS      = 500; // 启动豁免期
```

**保留**：`StallConfig_t` 现有字段（`retreatSteps / detectTicks / retreatTicks / enableExemptionMs`），只移除 `currentThresh_mA`。

### 4.4 电机端 main.cpp（35/42/57 同步）

**不需要初始化电流阈值**（用 `motionParams.ratedCurrent`，已经在 main.cpp 第 53 行设置过）：

```cpp
// 35/42/57 默认（与原来一样）
motor.config.motionParams.ratedCurrent = boardConfig.currentLimit;  // 主基线
// ratedCurrent = 1500 (35) / 2000 (42) / 2500 (57) mA 默认
// D4_IS_POWERING_RATIO = 75 → 阈值 1125 / 1500 / 1875 mA 默认
```

**用户改限幅时的链路**：

```
用户通过串口助手 CAN 0x12 修改 boardConfig.currentLimit
   ↓
motor.config.motionParams.ratedCurrent 自动跟随（同一变量）
   ↓
isPowering 阈值自动 = ratedCurrent × 75%
```

（旧的 `stallConfig.currentThresh_mA` 块删除，不再使用）

### 4.4 电机端 main.cpp（35/42/57 同步）

在默认块初始化：

```cpp
// 35/42 默认
stallConfig = { .currentThresh_mA = 1100,  // 42；35 用 900
               .retreatSteps = 35556,
               .detectTicks = 4000,
               .retreatTicks = 40000,
               .enableExemptionMs = 100 };

// 57 默认
stallConfig = { .currentThresh_mA = 1600,
               .retreatSteps = 51200,
               .detectTicks = 4000,
               .retreatTicks = 40000,
               .enableExemptionMs = 100 };
```

### 4.5 主控 ctrl_step.hpp / .cpp

**新增**：

```cpp
enum StallMode_t { IDLE = 0, RETREATING = 1, LOCKED = 2 };

class CtrlStepMotor {
    // ... 现有字段 ...
    StallMode_t stallMode = IDLE;                    // 来自 0x7C data[1]
    uint8_t motorIndex;                              // 0=地轨, 1~6=关节

    void SetAngleWithVelocityLimit(int32_t angle, uint16_t velLimit) {
        if (stallMode != IDLE) return;                // LOCKED/RETREATING 拒绝
        // ... 现有逻辑 ...
    }
};
```

**0x7C listener**：复用现有 CAN RX 路径，注册 0x7C 解包，更新 `stallMode`。

### 4.6 主控 can_protocol.cpp

3 处 0x7C 处理（地轨 / 关节 / 夹爪）全部重写：

```cpp
// 从 StdId 解析 nodeID
uint8_t id = (rxHeader.StdId >> 7) & 0x07;
uint8_t mode = rxData[1];

switch (mode) {
    case 2:  // LOCKED
        dummyRobot.SetStallMode(id);
        // 同时更新 motorStallMask[id]
        motorStallMask |= (1 << motorIndex);
        isStallMode = (motorStallMask != 0);
        break;
    case 0:  // IDLE
        dummyRobot.ClearStallMode(motorIndex);
        motorStallMask &= ~(1 << motorIndex);
        isStallMode = (motorStallMask != 0);
        break;
    case 1:  // RETREATING
        // 不动，等回退完成电机端再发 LOCKED
        break;
}
```

**Fallback**：主控每 100ms 发一次 `0x1B 轮询`（兜底 CAN 丢包导致 0x7C 丢失）。

### 4.7 主控 dummy_robot.h / .cpp

**新增字段**：

```cpp
// dummy_robot.h
extern uint8_t motorStallMask;       // 7 位掩码，bit0=地轨, bit1~6=关节
extern bool isStallMode;             // = (motorStallMask != 0)

void SetStallMode(uint8_t canID);                 // LOCKED 时调
void ClearStallMode(uint8_t motorIndex);          // IDLE 时调
```

**修改 SetEnable**：

```cpp
// SetEnable(false) 分支
_enable = false;
isStallMode = false;
motorStallMask = 0;
// ... 现有 disable 逻辑 ...

// SetEnable(true) 分支
_enable = true;
// 不清 stallMode；LOCKED 状态等电机端 enable=true 时清
```

**修复 C2/C3**：MoveJ/Homing/Resting 阻塞循环加 stall 检查 + 10s 超时：

```cpp
while (IsMoving() && IsEnabled() && !isStallMode && (millis() - t0 < 10000)) {
    osDelay(10);
}
```

### 4.8 主控 ascii_protocol.cpp


| 命令                           | 处理                                                                      |
| ---------------------------- | ----------------------------------------------------------------------- |
| `>j1,...,j6,speed` (MoveJ)   | `isStallMode` 时**返回错误** "STALL: send `!START` / enable / disable first" |
| `@x,y,z,a,b,c,speed` (MoveL) | 同上                                                                      |
| `!STALL_STATUS`              | 输出 `ok STALL_STATUS rail=0 j1=1 ... j6=0`（按 motorStallMask 解析）          |
| `!STALL_IGNORE`              | **保留**（跳过当前 stall 状态继续执行？**此处需用户确认**——已删除 ❓）                            |


> 注：`!STALL_IGNORE` 在 60 项清单 #38 标记为已通过，但语义与新设计冲突（disable 才失能）。**待用户最终拍板**。

### 4.9 RGB 心跳模式

触发堵转后（电机端 LOCKED 时）：

```cpp
// 主控收到 0x7C LOCKED → 设置 RGB 心跳
rgbController.SetMode(RGB_HEARTBEAT);
// 当 motorStallMask 清零时切回原模式
rgbController.RestoreMode();
```

**实现位置**：`dummy_robot.cpp::SetStallMode` 入口 + `ClearStallMode`（最后一个电机清时）出口。

---

## 5. 用户 60 项决策核对清单（冻结）

> ⚠️ **本清单是过去几天决策的完整快照，任何条目都不再讨论。**


| #   | 决定                                                                            | 来源                     | 状态                         |
| --- | ----------------------------------------------------------------------------- | ---------------------- | -------------------------- |
| 1   | 现有 1 秒延迟判定**必须删**                                                             | 用户原话                   | ✅                          |
| 2   | isStalled 锁死电机**必须改**                                                         | 用户原话                   | ✅                          |
| 3   | **回退→锁死→手动解除**三段式语义                                                           | 用户原话（2026-08-21 23:19） | ✅                          |
| 4   | 检测算法：电流+速度+误差 三元组                                                             | 用户原话 + AI 建议           | ✅                          |
| 5   | 运算开销 < 0.1%（20kHz）                                                            | 用户原话                   | ✅                          |
| 6   | 200ms 触发延迟（全部电机统一）                                                            | 用户确认 Q2                | ✅                          |
| 7   | 回退距离：j1~j5 = 35556 步，j6 = 21333 步，57 = 51200 步                                | 用户确认 Q1                | ✅                          |
| 8   | ~~电流阈值：35=900mA, 42=1100mA, 57=1600mA~~ → **改为 `ratedCurrent × 75%`**（详见 1.7） | 用户 2026-08-23 24:00 修订 | ✅                          |
| 9   | 回退速度 35/42 = 300 步/200us，57 = 600 步/200us                                     | 用户确认 Q4                | ✅                          |
| 10  | ~~`0x1D STALL_RESET` 命令码~~                                                    | ~~AI 建议~~              | ❌ **删除**                   |
| 11  | `!STALL_STATUS` / `!STALL_IGNORE`（**取消 !STALL_RESET**）                        | 用户决定                   | ✅                          |
| 12  | LOCKED 状态保留 PID I 项（保位）                                                       | 用户决定                   | ✅ 含义：LOCKED 期间不清 I 项，靠 P+DCE 重积分保位（入口/解除时各清一次，见 Q1 A1） |
| 13  | 0x89 急停清 stallMode（覆盖一切）                                                      | 用户决定                   | ✅                          |
| 14  | RETREATING 期间拒绝 0x03~0x07                                                     | 用户决定                   | ✅                          |
| 15  | lastMoveDirection 初始 +1                                                       | 用户决定                   | ✅                          |
| 16  | ENABLE 0x01 LOCKED 时 softNewCurve=true 切回 POSITION                            | 用户决定                   | ✅                          |
| 17  | ENABLE 0x01 LOCKED 时 goalVelocity=estVelocity                                 | 用户决定（Q6）               | ✅                          |
| 18  | DISABLE 0x01=false 时只清 stallMode（不强制 Sleep）                                   | 用户决定                   | ✅                          |
| 19  | `ClearStallFlag()` 函数保留                                                       | 用户决定（2026-08-23）       | ✅                          |
| 20  | 主控 `motorStallMask[7]` 按位管理                                                   | AI 补充                  | ✅                          |
| 21  | `isStallMode = (motorStallMask != 0)`                                         | AI 补充                  | ✅                          |
| 22  | 主控 `ClearStallMode(motorIndex)` 新方法                                           | AI 补充                  | ✅                          |
| 23  | CtrlStepMotor 新增 `enum StallMode_t` 字段                                        | AI 补充                  | ✅                          |
| 24  | CtrlStepMotor 不接收 0x7C 回包 → 加 listener                                        | AI 补充                  | ✅                          |
| 25  | can_protocol.cpp 三处 0x7C 全部重写                                                 | AI 补充                  | ✅                          |
| 26  | 0x7C 上报丢失 fallback = 0x1B 轮询 100ms                                            | AI 补充                  | ✅                          |
| 27  | `stallRetreatSteps` 字段：35/42=35556, 57=51200                                  | 用户决定                   | ✅                          |
| 28  | `stallCurrentThreshold` 由 main.cpp 默认块固化                                      | 用户决定                   | ✅                          |
| 29  | 启动豁免期 100ms                                                                   | AI 补充                  | ✅                          |
| 30  | LOCKED 入口一次性 ClearIntegral                                                    | 用户拍板 A1（替代早期 A2 草案）| ⏳ 待实施（Q1 决策，2026-08-24）|
| 31  | RETREATING 期间不清 I 项                                                           | 用户决定                   | ✅                          |
| 32  | 解除 stall 时（IDLE）再次 ClearIntegral                                              | AI 补充                  | ✅                          |
| 33  | softNewCurve 路径只在 IDLE 清 stall                                                | AI 补充（C8）              | ✅                          |
| 34  | encoder_calibrator 校准时强制 stallMode=IDLE                                       | AI 补充（C1/C6）           | ✅                          |
| 35  | 0x07 修改 ratedVelocity 时临时切换                                                   | AI 补充（C12/C13）         | ✅                          |
| 36  | RETREATING 也走 CtrlStepMotor stallMode 拦截                                      | AI 补充                  | ✅                          |
| 37  | CtrlStepMotor stallMode 用 enum 而非 uint8_t                                     | AI 补充                  | ✅                          |
| 38  | `!STALL_IGNORE` 跳过 stall 状态继续执行                                               | 用户决定                   | ⚠️ **待最终确认**（与新设计冲突）       |
| 39  | `!STALL_RESUME` 命令                                                            | ~~AI 建议~~              | ❌ **删除**（2026-08-21 23:57） |
| 40  | 0x1D STALL_RESET 命令                                                           | ~~AI 建议~~              | ❌ **删除**                   |
| 41  | 电机端不实现 `stall_protect_var.set()` Python 协议                                    | ~~AI 建议~~              | ❌ **删除**                   |
| 42  | 极简原则：1 个字段、0 个新 CAN 命令（除 0x7C/0x1B）                                           | 用户决定（2026-08-23）       | ✅                          |
| 43  | 回退速度复用电机内 `velocityLimit` 字段                                                  | 用户决定                   | ✅                          |
| 44  | j6 实际回退 8.3°（不是 5°）**用户接受**                                                   | 用户决定                   | ✅                          |
| 45  | `stallMode` 与 `motorStallMask` 对应关系                                           | AI 补充                  | ✅                          |
| 46  | `motorIndex` 参数语义是数组下标（0~6）                                                   | AI 补充                  | ✅                          |
| 47  | `ClearStallMode` 实现需要临界区保护                                                    | AI 补充                  | ✅                          |
| 48  | `isStallMode` 是 `dummy_robot` 7 轴统一标志                                         | AI 补充                  | ✅                          |
| 49  | MoveJ 阻塞循环加 stall 检查 + 10s 超时（C2）                                             | AI 补充                  | ✅                          |
| 50  | SetEnable(false) 清 motorStallMask（C3）                                         | AI 补充                  | ✅                          |
| 51  | 0x7C 解析用 enum StallMode_t（C47 修正）                                             | AI 补充                  | ✅                          |
| 52  | 主控 CAN RX 中断 vs 主循环读需要临界区保护                                                   | AI 补充                  | ✅                          |
| 53  | 0x1B 轮询 fallback 频率 100ms                                                     | AI 补充                  | ✅                          |
| 54  | RGB 心跳模式触发于 LOCKED 状态                                                         | 用户决定（2026-08-23 19:00） | ✅                          |
| 55  | enable 解除 LOCKED 后保持当前姿态                                                      | 用户决定（2026-08-23 19:00） | ✅                          |
| 56  | disable 解除 LOCKED 后失能电机                                                       | 用户决定（2026-08-23 19:00） | ✅                          |
| 57  | j1-j6 回退 5°，j6 因 30:1 允许多回退一些                                                 | 用户决定（2026-08-23 19:00） | ✅                          |
| 58  | 地轨电机回退 5mm                                                                    | 用户决定（2026-08-23 19:00） | ✅                          |
| 59  | 回退完保存当前姿态、更新角度                                                                | 用户决定（2026-08-23 19:00） | ✅                          |
| 60  | 重构目的放文档开头                                                                     | 用户决定（2026-08-23 19:00） | ✅                          |
| 61  | 电流阈值改用 `ratedCurrent × D4_IS_POWERING_RATIO / 100`（取消绝对 mA）                   | 用户决定（2026-08-23 24:00） | ✅                          |
| 62  | `D4_IS_POWERING_RATIO = 75`（编译时常量，不进 EEPROM，不加 CAN 命令）                        | 用户决定（2026-08-23 24:00） | ✅                          |
| 63  | 1A 限幅场景仍能检测堵转（57 = 1000mA → 阈值 750mA，堵转电流被限幅卡在 1A > 750）                      | 验证结论（2026-08-23）       | ✅                          |


---

## 6. 测试与验证清单

### 6.1 触发场景验证


| #   | 测试                      | 期望                                                       |
| --- | ----------------------- | -------------------------------------------------------- |
| T1  | MoveJ 时人工阻挡 J1 关节       | 200ms 后电机回退 5° → LOCKED → RGB 心跳 → 主控 `isStallMode=true` |
| T2  | MoveJ 时人工阻挡地轨           | 200ms 后地轨回退 5mm → LOCKED → RGB 心跳                        |
| T3  | MoveJ 时人工阻挡 J6          | 回退 21333 步（≈8.3° 电机端）→ LOCKED                            |
| T4  | 力矩模式运行后堵转               | 同样触发 LOCKED                                              |
| T5  | 重力负载下进入 LOCKED 5 秒      | 电机**不掉落**（PID 保位）                                        |
| T6  | LOCKED 状态下发 MoveJ       | 主控**立即返回** "STALL: send enable/disable first"            |
| T7  | LOCKED 状态下发 `enable`    | stallMode=IDLE + 电机保持当前姿态 + RGB 恢复                       |
| T8  | LOCKED 状态下发 `disable`   | stallMode=IDLE + 电机失能 + 可自由转动                            |
| T9  | RETREATING 期间发 `enable` | 等回退完成后再清 stall（不能中断回退）                                   |
| T10 | 0x89 急停后 stallMode      | 立即 IDLE + Brake                                          |


### 6.2 边界条件验证


| #   | 测试                     | 期望                             |
| --- | ---------------------- | ------------------------------ |
| B1  | ENABLE 后 100ms 内堵转     | **不触发**（启动豁免期）                 |
| B2  | 慢速运动（< 30 步/200us）     | **不触发**（MIN_SPEED 判据）          |
| B3  | 小位置误差（< 200 步）         | **不触发**（STALL_ERROR_THRESH 判据） |
| B4  | 断电后重启电机                | stallMode=IDLE（不持久化）           |
| B5  | encoder_calibrator 校准中 | stallMode 强制 IDLE              |
| B6  | `softNewCurve=true` 路径 | LOCKED 时**不**清 stall（C8）       |
| B7  | 回退超时（> 2s 不到位）         | 强制 LOCKED + 发 0x7C             |
| B8  | 0x7C CAN 丢包            | 100ms 内 0x1B 轮询兜底              |


### 6.3 文档结尾验证条目（重构完成后快速回归）


| #   | 验证项                    | 期望                   |
| --- | ---------------------- | -------------------- |
| V1  | 编译 0 error             | `ninja` 三个 build/ 目录 |
| V2  | 编译 RAM/Flash 使用率合理     | < 80%                |
| V3  | 文档 60 项清单逐条对照代码        | 全 ✅                  |
| V4  | 偏差表 17 项 ❌ 全部修复        | 编译通过 + 运行时验证         |
| V5  | 夹爪固件（gripper）未改动       | git diff 无变化         |
| V6  | `!STALL_IGNORE` 语义最终拍板 | 待用户决定                |
| V7  | T1~T10 实测全部通过          | 至少 T1/T2/T7/T8       |


---

## 7. 当前偏差表（代码与文档要求的差距）

> 偏差表说明：**✅ 合规** / **⚠️ 部分合规** / **❌ 不合规（必须修复）**


| ID    | 文档要求                                     | 当前代码                                                | 状态   |
| ----- | ---------------------------------------- | --------------------------------------------------- | ---- |
| 偏差-1  | 60 项核对清单 #1：删除 1 秒判定                     | motor.cpp:284 `stalledTime >= 1000*1000`            | ✅ 已删 |
| 偏差-2  | 60 项 #2：isStalled 不锁死                    | motor.cpp:105-110 仍跳 switch case                    | ❌    |
| 偏差-3  | 60 项 #4：D4 三元组                           | motor.cpp:273-281 仅电流                               | ❌    |
| 偏差-4  | 60 项 #3：回退→锁死                            | 无状态机                                                | ❌    |
| 偏差-5  | 60 项 #5：运算开销                             | n/a（合规）                                             | ✅    |
| 偏差-6  | 60 项 #6：200ms 触发                         | STALL_DETECT_TICKS=4000                             | ✅    |
| 偏差-7  | 60 项 #7：回退距离                             | main.cpp: stallRetreatSteps=711 (35/42)、204800 (57) | ❌    |
| 偏差-8  | 60 项 #8：电流阈值走 `ratedCurrent × 75%`       | motor.cpp 第 14 行 k=75% 已落实（已替换原 95% ratedCurrent）   | ✅    |
| 偏差-9  | 60 项 #11：`!STALL_STATUS`                 | dummy_robot.cpp:564 输出格式不符                          | ❌    |
| 偏差-10 | 60 项 #12/30/32：LOCKED PID 保位             | 无 ClearIntegral + LOCKED                            | ⏳ Q1 A1 待实施                          |
| 偏差-11 | 60 项 #13：0x89 清 stall                    | interface_can.cpp 0x89 不调 ClearStallFlag            | ❌    |
| 偏差-12 | 60 项 #22：MoveJ 拦截                        | ascii_protocol.cpp L791-796                         | ⚠️   |
| 偏差-13 | 60 项 #25：`!STALL_STATUS` 格式              | 输出 `ok STALL_STATUS rail_en=...` 不一致                | ❌    |
| 偏差-14 | 60 项 #47：CtrlStepMotor stallMode         | ctrl_step.hpp 缺字段                                   | ❌    |
| 偏差-15 | 60 项 #58：encoder_calibrator 入口           | interface_can.cpp 0x02 仅 isTriggered                | ⚠️   |
| 偏差-16 | 60 项 #50：ENABLE 切 POSITION               | interface_can.cpp L38-39 仅切 VELOCITY                | ❌    |
| 偏差-17 | 60 项 #18：DISABLE 清 stall                 | enable=false 不调 ClearStallFlag                      | ❌    |
| 偏差-18 | 60 项 #4：RETREATING 期间 motion planner     | motor.cpp L348 每周期 SetPositionSetPoint              | ❌    |
| 偏差-19 | 60 项 #30：ClearIntegral 一次性               | 偏差-10 重述（Q1 A1 待实施）                                | ⏳                          |
| 偏差-20 | 60 项 #17：ENABLE goalVelocity=estVelocity | interface_can.cpp L38-39 未调 SetVelocitySetPoint     | ⚠️   |
| 偏差-21 | 60 项 #48：CtrlStepMotor 0x7C listener     | ctrl_step 无 0x7C listener                           | ❌    |
| 偏差-22 | 60 项 C2：MoveJ 阻塞循环 stall 检查              | dummy_robot.cpp:507 `while(IsMoving())` 死循环         | ❌    |
| 偏差-23 | 60 项 C3：SetEnable(false) 清 mask          | dummy_robot.cpp:534 disable 分支不清                    | ❌    |
| 偏差-24 | 60 项 #51：ClearStallFlag 上报 0x7C          | motor.cpp:608 ClearStallFlag 不发 0x7C                | ❌    |
| 偏差-25 | 60 项 #57：encoder_calibrator 强制 IDLE      | encoder_calibrator 入口未强制清                           | ⚠️   |
| 偏差-26 | 60 项 #44：电机端 LOCKED 入口 ClearIntegral     | 偏差-10/19 重述（Q1 A1 待实施）                             | ⏳                          |
| 偏差-27 | 60 项 #29：启动豁免期 100ms                     | 无 `enableTimestamp`                                 | ❌    |
| 偏差-28 | 60 项 #43：reachedRetreat 阈值               | 未单独提取（用 motion_planner FINISH）                      | ✅    |


---

## 文档维护说明

- **本重构冻结日期**：2026-08-23
- **下次决策变更**：不允许（60 项清单已冻结）
- **下一阶段**：固件重构（电机端 35/42/57 → 主控）
- **变更日志**：
  - 2026-08-23 19:00：v2.0 重写版，4950 行 → 800 行
  - 2026-08-24 00:40：v2.1 增量更新（Q1/Q2 用户拍板）
    - 偏差表顶部新增 2026-08-24 决策记录块（Q1 A1、Q2 阈值）
    - 偏差-10/19/26：❌ → ⏳（待实施 Q1 A1）
    - 偏差-28：标注 Q2 用户拍板（待实施改 5 步）
    - 1.3 节 Q7：注明 A1 决策 + 三个时间点清 I 项逻辑
    - 2.3 节时序图：补充 5 步阈值 + Q2 决策注
    - 2.4 节保位机制：补充"入口清脏 I 项"理由 + motor.cpp 实施位置
    - 4.x 60 项表 #5：标注 Q1 A1 待实施
    - 4.x 60 项表 #12：说明 "保留 I 项" 的精确定义（入口/解除时各清一次）
    - 4.x 60 项表 #30：✅ → ⏳（Q1 A1 待实施）
  - 重构完成后归档到 git commit

